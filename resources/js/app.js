import.meta.glob(["../images/**"]);

// AlpineJS is already loaded via CDN in the layout <head>,
// but if we switch to bundling it later, we would import it here.
// For now, this file is clean and ready for custom JS.
console.log("T.A.L.A. Student Portal JS Loaded");
