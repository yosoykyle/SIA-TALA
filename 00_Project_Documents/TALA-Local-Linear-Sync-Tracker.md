# TALA Local Linear Sync Tracker

## Purpose

This document is the local staging area for completed issues awaiting explicit user-authorized Linear synchronization, and a compact reference for issues already synced.
- After primary verification, record completed work under Active Syncs as `Done locally; pending explicit Linear sync` and create its bounded local Git commit.
- This file never triggers Linear automatically. Do not create, update, comment on, or otherwise synchronize a Linear issue until the user explicitly says `Sync TAL-XX to Linear` or gives an equally explicit issue-specific Linear instruction.
- `Finish`, `close`, `cleanup`, `commit`, or `proceed` alone leave the task pending locally.
- Once explicitly synced, move the row to Compact Synced History and keep only the ID, Linear status, and title.
- Planning remains in `TALA-Rescue-Next-Steps.md`; completed local work belongs here, not in the planning table.

## Current Linear Snapshot

## Active Syncs

| Issue | Local Status | Title / Domain |
| --- | --- | --- |
| TAL-79 | Protocol amendment pending explicit Linear sync | Explicit local-commit and Linear-authorization boundary |
| TAL-80 | Done locally; pending explicit Linear sync | Foundation Acceptance Map and Slice Sequencing |
| TAL-81 | Done locally; pending explicit Linear sync | Identity, Access, Workspace, and Admin Baseline Acceptance |
| TAL-82A | Done locally; pending explicit Linear sync | Academic Setup Core Surfaces Acceptance |
| TAL-82B | Done locally; pending explicit Linear sync | Academic Setup Curriculum and Course Catalog Acceptance |
| TAL-82C | Done locally; pending explicit Linear sync | Academic Calendar Windows and Grade Window Inputs Acceptance |
| TAL-82D | Done locally; pending explicit Linear sync | Guarded Course Specification and Curriculum Import Template Acceptance |
| TAL-83A | Done locally; pending explicit Linear sync | Admissions Intake and Registrar Review Acceptance |
| TAL-83B | Done locally; pending explicit Linear sync | Applicant-to-Student Handover Acceptance |
| TAL-83C | Done locally; pending explicit Linear sync | Duplicate Profile Resolution Acceptance |
| TAL-83D | Done locally; pending explicit Linear sync | Student Profile and Student Hub Activation Smoke |
| TAL-84A | Done locally; pending explicit Linear sync | Holds and Lifecycle-Status Contract |
| TAL-84B | Done locally; pending explicit Linear sync | Core Lifecycle Recorded Results Acceptance |
| TAL-84C | Done locally; pending explicit Linear sync | Program-Shift Credit Evaluation Acceptance |
| TAL-84D | Done locally; pending explicit Linear sync | Academic Standing and Student Unit-Load Exceptions Acceptance |
| TAL-85A | Done locally; pending explicit Linear sync | Term Offering and Section Delivery Group Source-Record UI Alignment |
| TAL-85B | Done locally; pending explicit Linear sync | Resource Readiness Surfaces |
| TAL-85C | Done locally; pending explicit Linear sync | Scheduling Demand Readiness Acceptance |
| TAL-85D | Done locally; pending explicit Linear sync | Master Schedule Publication and Official Meetings Acceptance |
| TAL-86A | Done locally; pending explicit Linear sync | Fee Rules and Assessment Activation Acceptance |
| TAL-86B | Done locally; pending explicit Linear sync | Manual Cashier Payment, OR Mapping, and Payment Evidence Ledger Posting Acceptance |
| TAL-86C | Done locally; pending explicit Linear sync | Ledger Adjustment and Reversal Cleanup Acceptance |
| TAL-86D | Done locally; pending explicit Linear sync | Financial Accommodation and Promissory-Effect Acceptance |
| TAL-86E | Done locally; pending explicit Linear sync | Finance Gate Source Behavior Smoke |
| TAL-87A | Done locally; pending explicit Linear sync | Clean Enrollment Source-Record Baseline |
| TAL-87B | Done locally; pending explicit Linear sync | Staff Gate Review Surface |
| TAL-87C | Done locally; pending explicit Linear sync | Enrollment Gate Evaluator and Exception Gate Acceptance |
| TAL-87D | Done locally; pending explicit Linear sync | Official Enrollment Finalization Acceptance |
| TAL-88A | Done locally; pending explicit Linear sync | COR Source-Output Acceptance |
| TAL-88B | Done locally; pending explicit Linear sync | Finance Official Outputs Acceptance |
| TAL-88C | Done locally; pending explicit Linear sync | Stale Public COR Verification Deferral |
| TAL-88D | Done locally; pending explicit Linear sync | Cross-Role Output Regression |
| TAL-89A | Done locally; pending explicit Linear sync | Faculty Roster Entry and Registrar Post/Release Acceptance |
| TAL-89B | Done locally; pending explicit Linear sync | Late Grade Authorization and Returned-Roster Re-Entry Acceptance |
| TAL-89C | Done locally; pending explicit Linear sync | INC Completion/Removal and Posted Correction Acceptance |
| TAL-89D | Done locally; pending explicit Linear sync | Student Released-Grade Visibility and Cross-Role Grade Regression |
| TAL-90A | Done locally; pending explicit Linear sync | Progression and Standing Acceptance |
| TAL-90B | Done locally; pending explicit Linear sync | Graduation Batch and Snapshot Acceptance (incl. PRD §11.3.1 rules 5-7 clarification) |
| TAL-90C | Done locally; pending explicit Linear sync | Staff Visibility and Student-Safe Completion Regression (incl. PRD §11.3.1 rule 9 inactive-membership retraction) |
| TAL-91A | Done locally; pending explicit Linear sync | Student Hub Dashboard Priority-Notice Acceptance (PRD §12.2 tiers 1/2/3/5/11; native `databaseNotifications()` + notifications table added) |
| TAL-91B | Done locally; pending explicit Linear sync | Student-safe Finance Projection and Official-Output Access Logging Acceptance (PRD §12.1 item 14 accommodation staff-field hiding, page-level own-records isolation, §12.2 rule 9 payment/ledger/OR-mapping state distinction; test-only, no production leak found) |
| TAL-91C | Done locally; pending explicit Linear sync | Academic Outputs Projection Acceptance: fixed `ScheduleView` published+active meeting filter gap (candidate/superseded schedule leak); added Student Hub Capacity Pending (§12.2 tier 4) and Pending-Review gate-reason (§12.2 rule 8) tiers, reusing already-persisted `EnrollmentGateResult` via `EnrollmentGateReviewSummary`; COR acceptance-only (already covered by TAL-70/88A) |
| TAL-91D | Done locally; pending explicit Linear sync | Academic Status Student-Safe Regression (final TAL-91 sub-slice): added Holds view "Office to Contact" column reusing `Hold::studentFacingOfficeLabel()` (PRD §12.2 rule 2); surfaced `StudentProfile::academic_standing` on Lifecycle view and in `StudentDashboardService::profile()` (PRD §12.1 item 11); grades/completion re-verified as regression only (already built/tested by TAL-89D, `StudentHubCompletionReviewTest`). Parent TAL-91 (Student Hub Projection Acceptance) is now complete: 91A-91D all done. Deferred: resolver display-priority tiers 6-10 routed to new TAL-91E. |
| TAL-91E | Done locally; pending explicit Linear sync | Student Hub Display Priority Completion: implemented the final 5 of 11 ranked notice tiers (§12.2 ranks 6-10 - Missing requirements, Active academic deficiency, Schedule available, COR available, Grades released) in `StudentHubPriorityResolver`. Each tier reuses an already-existing, already-student-exposed data source (`ChecklistItem::isResolved()`, `StudentProfile::academic_standing` excluding Irregular, `StudentDashboardService::scheduleFor()`'s filter shape, `BuildCorOutput::forStudent()` directly, `GradesView::releasedGradesQuery()`'s filter shape) rather than new logic, avoiding the divergence-bug class fixed in TAL-91C. All 11 §12.2 tiers now implemented; none remain deferred. |
| TAL-92A | Done locally; pending explicit Linear sync | Fixed Reports & Export Audit Acceptance (first sub-slice of parent TAL-92): accepted the existing `OperationalReportService`/`ExportOperationalReport`/`OperationalReportPolicy`/`ReportsAudit` implementation against PRD §13.3; built the missing Curriculum Version Report catalog entry and granted Academic Head visibility into the existing Graduation Eligibility Snapshot report (additive-only; Registrar access unaffected, Accounting still rejected), bringing the Academic Head catalog from 9 to 11 to match the PRD's enumerated report list. Reconciled PRD §13.3.6 (`report_export_log` naming vs the implemented unified `output_access_logs` table) and recorded deferral destinations for Sensitive/Document/Login-Session Audit breakdowns (→ TAL-92B), Solver Run History (→ TAL-94), PayMongo Webhook Event Log (→ TAL-95), and Privacy Request Log (→ TAL-92F). |
| TAL-92B | Done locally; pending explicit Linear sync | Audit Trail Coverage Acceptance (second sub-slice of parent TAL-92): benchmarked `spatie/laravel-activitylog` as the correct package choice (48M+ installs, PRD-named, already used at ~25 call sites) — no package change. Accepted the existing read-only, Super-Admin-scoped `ActivityResource`/`ActivityPolicy` against PRD §13.6/§13.8. Closed 3 confirmed logging gaps: login/session events via new `LogAuthenticationActivity` listener (password never captured, verified by explicit test assertion), Graduation Review Batch/membership/snapshot/visibility events, and Holds create/expire/resolve/waive events. Confirmed the Sensitive/Document/Login-Session Audit distinct-breakdown reports (PRD §13.3.4 #2/#3/#9) are satisfied by consolidated coverage, no separate reports needed. Benchmarked `pxlrbt/filament-activity-log` (zero references) against the hand-built `ActivityResource`: hand-built wins on simplicity/maintainability; flagged for TAL-93 removal. Added a PRD §13.6 note recording that V1 audit immutability is application-layer (read-only policy), not DB-level tamper-evident; DB-level hardening deferred to TAL-93. |
| TAL-92C | Done locally; pending explicit Linear sync | Guarded Imports Acceptance (third sub-slice of parent TAL-92): benchmarked the custom `AcademicImportService`/`ImportBatch` state machine against Filament's native `ImportAction` (does not fit the guarded preview/warning-gate/draft-only-then-activate contract) and mature staged-import practice (Atlassian, Snowflake) — confirmed correct, no package change. Accepted the existing implementation against PRD §13.4 (16 existing tests already covered nearly the full contract). Added a CSV formula-injection guard to `AcademicImportCsv` for consistency with TAL-92A's export guard. Reconciled PRD §13.4.2 (synchronous 3-state persisted model `PENDING_REVIEW`/`POSTED`/`CANCELLED` vs the 6 PRD-named states; "Posted" = "Draft Created"). Extended `TAL82DImportTemplateAcceptanceTest` 16→19 tests (non-UTF-8 rejection, import audit-log capture/visibility, findings-CSV injection-safety). Deferred a `maatwebsite/excel` usage audit to TAL-93 alongside the `pxlrbt` removal decision. |
| TAL-92D | Done locally; pending explicit Linear sync | Integration Status & Operational Monitoring (fourth sub-slice of parent TAL-92, Direction A after re-plan): investigation of `config/tala_integrations.php`/`config/paymongo.php`/`config/mail.php` proved every integration is driver-switchable via environment variables with secrets living only in `.env`/`config/`, never the database — ruled out an editable DB-backed settings form as contradicting the PRD's own "secrets by secure reference" rule. Accepted `SystemSettingResource` as-is. Built a read-only `IntegrationStatus` page (driver/mode, configured-or-not derived from key presence never value, non-secret reference fields) plus one safe "Send test email" action hard-targeted to only the acting admin's own address. Built a read-only `OperationalEventResource` (+ policy + factory) monitoring integration events/failures, mirroring the accepted `ActivityResource` pattern; retry deferred (no live producers exist yet — TAL-94/95). OCR intentionally excluded and shown as an honest "not yet wired" placeholder (no config wiring exists for it; not in PRD §13.5 scope). Reconciled PRD §13.5.1. 9 new tests/103 assertions; regression clean. |
| TAL-92E | Done locally; pending explicit Linear sync | Retention Categories + Disposal Review (fifth sub-slice of parent TAL-92, Build, Direction A — disposal-review ledger, no hard deletion; private HEI — internal Records-Officer/Super-Admin disposal authority, confirmed by user). Preceded by a docs-only clarification: added PRD §13.7.5 (Record Accessibility and Archival Tiers) answering whether aged/previous-batch records stay accessible — policy is category+period+hold driven, not age driven; permanent records stay directly accessible indefinitely (Philippine HEI practice + CHED 15-day TOR rule); V1 archival is soft/in-database; physical/offline archival deferred to new issue TAL-98. Built `RetentionCategory` enum + `config/tala_retention.php` reference map (all PRD §13.7.1–13.7.3 record types classified); a read-only `DisposalReviewResource` disposal-candidate table (model `StudentProfile` scoped to `archived_at IS NOT NULL`, i.e. rejected/merged duplicate profiles — the only in-scope candidate source this slice); one `reviewDisposal` confirmation action mirroring the accepted `IntegrationStatus::sendTestEmail` template, which runs `HoldEvaluationService::hasActiveBlockingHold()` unconditionally before any attestation check — an active hold always forces `BLOCKED_BY_HOLD` regardless of attestation (PRD §13.7.4 rule 8, "not optional"); writes one `DisposalReview` ledger row + one `activity_log` entry per action, success or blocked. New `DisposalReviewPolicy` (Super-Admin-only view/create; update/delete always false — audit trail, not editable). 7 new tests/108 assertions (independently reproduced by primary: 26/26 across new+regression files, 308 assertions, plus one additional regression file the worker hadn't cited). Deferred: automated disposal jobs (rule 10 → TAL-98), per-type retention-period editing UI, legal/audit-hold DB tables (handled by attestation), physical/offline archival + archive-management UI (§13.7.5 point 7 → TAL-98). |
| TAL-92F | Done locally; pending explicit Linear sync | Remaining System Configuration + Notification Acceptance + parent closure (sixth and final sub-slice of parent TAL-92; parent-closing catch-all). Config-governance acceptance test (`TAL92FSystemConfigurationAcceptanceTest`, 2 tests/36 assertions) proving what TAL-92D did not: (A1) PRD §13.1.2 governance semantics on `system_settings` — two coexisting versions of one key (superseded v1 + active v2) with distinct effective windows, preserved historical value, exactly one active version, and actor (`changed_by`) + `reason` traceability; (A2) PRD §13.1.1 item 16 role-permission configuration is read-only (`RoleResource::canCreate()===false`) and Super-Admin-only via `RolePolicy` (all mutations false; `ListRoles` forbidden to the other four staff roles) — `RolePolicy` was previously uncovered. Deliberately did NOT re-test the TAL-92D `SystemSettingResource`/`SystemSettingPolicy`/`operational_events` surfaces (accepted, not duplicated). Two PRD reconciliations (Authority Document Correction rule, approved in plan): §13.3.4 #10 Privacy Request Log → routed to new post-MVP issue TAL-99 after a focused RA 10173 §16 benchmark (data-subject-request handling is a DPO-owned manual/hybrid process; no source record; RA 10173/IRR mandate no in-system request-log UI; no MVP dependency); §13.2 note — V1 notification content is code-defined Laravel Mailables (DB-configurable templates §13.1.1 #17 deferred as post-MVP → new issue TAL-100), delivery metadata captured by `operational_events` (TAL-92D), integration-dependent production triggers (Payment Received, Schedule Released) wired under TAL-95/TAL-94, and the student-facing Student Hub priority notices (TAL-91A, `notifications` table) noted as a separate projection surface. Parent-closure integrity fix (primary, during independent verification): `SchemaConformanceTest` clean-MVP inventory corrected — added `notifications` (legit/used, TAL-91A) to platform tables and `disposal_reviews` (TAL-92E) to application tables, bumped counts 58→59 / 17→18 / 75→77; this test had been red since TAL-91A and was missed by prior feature-focused regression sweeps. Independently verified by primary: new test + `SchemaConformanceTest` + `TAL92DIntegrationMonitoringTest` + `TAL92ERetentionDisposalTest` + `TAL91AStudentHubDashboardProjectionAcceptanceTest` = 33 passed / 496 assertions; Pint clean; no `app/` file changed (Larastan N/A); `git diff --check` clean. **Parent TAL-92 complete — all six sub-slices (A–F) done locally, pending explicit Linear sync.** |
| TAL-93A | Done locally; pending explicit Linear sync | Foundation Housekeeping (first sub-slice of parent TAL-93, pre-integration gate). Removed two unused dependencies with zero application references: `pxlrbt/filament-activity-log` (hand-built read-only `ActivityResource` supersedes it) and `maatwebsite/excel` (imports/exports use native `fgetcsv`/`fputcsv`; deleted the published `config/excel.php` stub). Audit-immutability disposition (PRD 13.6): application-layer immutability (read-only `ActivityPolicy`, `canCreate()=false`) accepted as the proportionate V1 posture per a focused benchmark (DB-level append-only/hash-chaining is a compliance-grade going-further measure; the money trail in `ledger_entries` is separately protected by TAL-86C reversal-based no-hard-delete); DB-level tamper-evidence hardening routed to new post-MVP issue TAL-101. Authority docs reconciled: architecture spec (both packages) + PRD 13.6 note. Removal proven safe by primary: relevant tests green (imports TAL82D + reports/export TAL75 + audit TAL92B = 36 passed/440 assertions; isolated TAL82 4/4), composer validate clean, route:list boots (123 routes), zero dangling .php references, Pint clean, git diff --check clean; no app/ PHP changed. Two PRE-EXISTING whole-project debts surfaced during verification and routed to TAL-93B (not caused by this slice): (1) full-suite test-isolation conflict (6 RefreshDatabase vs 59 DatabaseTransactions over the shared seeded test_tala_db -> 30 RoleAlreadyExists failures); (2) whole-project Larastan = 407 errors at level 5 with no baseline. |
| TAL-93B | Done locally; pending explicit Linear sync | Test-Isolation Repair (2nd sub-slice of parent TAL-93). Orchestrated to one worker; independently verified by primary. Made role-seeding idempotent in 4 test files (StudentHubTest, TAL72GradesFilamentTest, TAL72GradesMvpTest, TAL82AcademicSetupAcceptanceTest): Role::create([name=>x]) -> Role::findOrCreate(x,web), matching the suite convention, the laravel-permission-development skill (findOrCreate is idempotent), and Boost Laravel 12 database-testing docs (RefreshDatabase only transaction-wraps an already-migrated DB, so seeded roles persist and non-idempotent create() collides). Verification (primary re-ran): full suite 30 failed -> 9 failed (544 passed, 4859 assertions), 0 RoleAlreadyExists, no regressions; diffs surgical (test files only); Pint passed; git diff --check clean. Worker returned PARTIAL and correctly flagged that the primary premise "all 30 are isolation" was inaccurate: 9 of the 30 are pre-existing GENUINE failures that also fail in isolation and need product/app changes (hard-excluded). Routed to new sub-slices TAL-93C (personal-data-correction lrn/birthdate schema-vs-model mismatch, 6 tests) and TAL-93D (verified-user 403 authorization regression, 3 tests). Roadmap re-sequenced: A,B done; C,D feature fixes; E Larastan baseline; F UAT gate (last). Exclusions honored: no product/app code, no isolation refactor, no Larastan work, no tests added/removed/weakened. |
| TAL-93C | Done locally; pending explicit Linear sync | Feature Fix — Retire legacy PersonalDataCorrectionRequest + align to PRD §3.5 (3rd sub-slice of parent TAL-93). Retired 8 non-functional legacy files (Model, Service, Filament Resource+Pages, Policy, Factory, Test) — feature contradicted §3.5, had no migration/table, referenced phantom lrn/birthdate columns, wrote to the immutable intake, and was unspecified in the PRD. Aligned to §3.5: added birth_date (DatePicker) + prior_identifier (LRN / Prior-Education Identifier) to the Registrar StudentProfileResource admin-override form; student self-service (phone/email/address/emergency) already existed. Added manual activity()->log() audit in EditStudentProfile::afterSave() and Student Profile::saveProfile() per §3.5 rule 2. Removed 3 already-skipped legacy stub tests from AdmissionsStudentHandoverUiTest. Created TAL93CProfileUpdateAlignmentTest (2 tests: registrar admin-override updates locked fields + audit; student self-service audit). Reconciled ui_surface_blueprint.md "profile correction" -> "Manual student profile updates (Admin Override)". Benchmark: RA 9048/10172 (corrections at civil registry, school records on evidence); DepEd LRN = K-12, optional for college, modeled as prior_identifier. Verification: focused 8/51 green; full suite 3 failed (TAL-93D auth 403s only) / 546 passed / 0 skipped (4872 assertions); Pint passed; Larastan 0 errors on changed files; route:list 123; git diff --check clean. |
| TAL-93D | Done locally; pending explicit Linear sync | Verified-User 403 Authorization Regression Fix (4th sub-slice of parent TAL-93). Diagnosed the three remaining full-suite failures as stale student-panel fixtures, not a production authorization bug: PRD/blueprint require Student Hub after handover, `User::canAccessPanel()` requires an active official `StudentProfile`, and `PanelAccessBoundaryTest` already asserts that an active student without an official profile is forbidden. Added StudentProfile fixtures only for positive student cases in `EmailVerificationBoundaryTest` and `RoleAwareLoginLandingTest`; no production auth code changed. Reconciled `TALA-Rescue-Next-Steps.md` stale TAL-93 boundary wording. Verification: final TAL-93D focused files 24 passed / 49 assertions; PanelAccessBoundary 34 passed / 37 assertions; WorkspaceAuthenticationEligibility 13 passed / 39 assertions; TAL-49 auth bundle 100 passed / 329 assertions; full suite 549 passed / 4879 assertions. Note: a parallel verification attempt collided on MySQL `test_tala_db` migration refreshes; the DB was reset with explicit testing/mysql/test_tala_db proof and subsequent checks were run sequentially. |
| TAL-93E | Done locally; pending explicit Linear sync | Static-Analysis Baseline (5th sub-slice of parent TAL-93). Generated `phpstan-baseline.neon` from current whole-project Larastan level 5 debt and included it from `phpstan.neon`, turning PHPStan into a new-error gate. Live inventory before baseline: 393 file errors (333 `property.notFound`, 60 non-property), superseding the earlier 407/344/~63 snapshot. Non-property triage found no safe isolated PHP cleanup within this tool slice; higher-signal findings are stale/cross-surface model/API mismatches or Eloquent/Larastan inference gaps and remain captured in the baseline rather than broadened into product fixes. Verification: `php -d memory_limit=1G vendor/bin/phpstan analyse --no-progress --memory-limit=1G` -> no errors; `composer validate --no-check-publish` valid with existing TallStackUI exact-version warning; `git diff --check` clean. No app PHP changed; PHPUnit/Pint not applicable. |
| TAL-93F1 | Done locally; pending explicit Linear sync | RETIRE step 1 - remove orphaned admissions-offering consumer surfaces (first of the TAL-93F RETIRE; diagnosed = superseded pre-rescue IAS remnant). Deleted 30 files of already-unregistered/unreachable dead code (absent from AdminPanelProvider explicit resources/pages arrays; zero routes): the AdmissionOfferings + AdmissionCapacityPlans + DocumentRequirementItems Filament resources (24 files), the AdmissionReadinessDashboard page + AdmissionReadinessDashboardService, the never-constructed AdmissionRequirementResolution DTO, and 3 orphaned policies. DEFERRED (verified): the 3 factories (AdmissionOfferingFactory->F3, AdmissionCapacityPlanFactory->F2, DocumentRequirementItemFactory->F3) and the 4 tableless models stay until F2/F3 because the surviving models still reference their factories via use + @use HasFactory annotations (deleting now would create dangling refs / new Larastan errors). Verification (primary, direct): composer dump-autoload OK (11,359 classes); route:list = 172 unchanged; full suite 549 passed / 4879 assertions / 0 failed / 0 skipped on test_tala_db; Pint passed; TAL-93E baseline regenerated 393->367 errors / 290->271 entries, diff = 114 deletions / 0 insertions (only dead-code entries removed, no new frozen errors); phpstan analyse -> [OK] No errors; git diff --check clean. No production behavior touched. Next: TAL-93F2 (finance-clearance unwire, HIGH-RISK). |
| TAL-93F2 | Done locally; pending explicit Linear sync | RETIRE step 2 - unwire the tableless admission-capacity path from finance clearance (2nd of the TAL-93F RETIRE; HIGH-RISK finance/enrollment). Removed the two AdmissionCapacity* calls (assertReadyForFinanceClearance + secureForFinanceClearedEnrollment) + 2 ctor deps + the now-unused Payment import from EnrollmentFinanceClearanceService (clearIfEligible signature UNCHANGED -> PayMongoWebhookProcessor/PaymentConfirmationService untouched); deleted 6 superseded files (AdmissionFinanceReadinessGateService, AdmissionCapacityReservationService, AdmissionCapacityPlan + AdmissionCapacityReservation models + their 2 factories, deferred from F1). Fixes a latent first-term-student finance-clearance crash (tableless admission_capacity_plans query). Benchmark (PRD 7 + reused TAL-93F retire benchmark): capacity is enforced by the LIVE EnrollmentGateEvaluator via EnrollmentSeatReservation (PRD 7.3 slot-rule 4) and FinalizeOfficialEnrollment re-runs the gate; Finance Gate = posted payment/downpayment OR active accommodation only (PRD 7.4) -> the removed calendar/scheduling/published-schedule bundling drops NO PRD feature. Added a first-term regression test (approved ApplicantIntake + verified payment -> clears, no crash) to TAL86EFinanceGateSourceBehaviorTest. Verification (primary, direct, test_tala_db): focused 8 passed/80 assertions; full suite 550 passed/4888 assertions/0 failed; Pint clean; TAL-93E baseline regenerated 367->338 errors / 271->249 entries (all removals); phpstan [OK]; route:list 172; php -l clean; git diff --check clean. Also normalized Next-Steps.md CRLF->LF per .gitattributes eol=lf (memory recorded). Next: TAL-93F3. |
| TAL-93F3 | Done locally; pending explicit Linear sync | RETIRE step 3 - remove the stale admission-requirement-policy admin UI + drop the last superseded models (final of the TAL-93F RETIRE; parent TAL-93F now COMPLETE). DECISION = REMOVE (not fix): AdmissionRequirementPolicyResource (8 files: Resource + 4 Pages + Form + Infolist + Table) was UNREGISTERED (absent from AdminPanelProvider) dead code built on the retired offering design and broken against the live model (undefined Status* constants; dead admissionOffering/documentRequirementItems/approver relations). Removed it + deleted the superseded AdmissionOffering + DocumentRequirementItem models + AdmissionOfferingFactory + DocumentRequirementItemFactory (12 files, zero code edits). KEPT the live state-based AdmissionRequirementPolicy + its factory + AdmissionRequirementResolver + ApplicantIntakeService + ChecklistItem (flat-checklist design per PRD 3.1). The PRD 3.1 + 13.1 admission-requirement-policy CONFIGURATION capability (live model has no working management surface) was reclassified as a CORE MVP sub-slice TAL-93I (Admission Requirement Policy Configuration, PRD 13.1.1 #6/#7/#8 + rules 7-8 + 3.1; depends on TAL-93G for the manage-admission-setup permission), not dropped; authority docs already describe the flat design so no PRD/blueprint/architecture correction was needed. Verification (primary, direct, test_tala_db): safety grep = exactly the 12 deletion-set files, no test/live/seeder refs; no migration + DB confirms admission_offerings/document_requirement_items ABSENT while admission_requirement_policies/checklist_items EXIST; full suite 550 passed/4888 assertions/0 failed; route:list 172 unchanged; dump-autoload -12 classes; TAL-93E baseline regenerated 338->327 errors / 249->239 entries (all removals); phpstan [OK]; Pint passed; git diff --check clean. Next: TAL-93G. |

## Compact Synced History

*Search these IDs on the Linear website for full details, descriptions, and evidence.*

| Issue | Status (Linear) | Title / Domain |
| --- | --- | --- |
| TAL-42 | Done | R0 PRD rebaseline rescue controller |
| TAL-43 | Done | Public landing page and Filament auth routing baseline |
| TAL-44 | Done | Applicant Workspace shell and navigation |
| TAL-45 | Done | Student Hub shell |
| TAL-46 | Canceled | Staff workspace navigation |
| TAL-47 | Canceled | Frontend smoke tests |
| TAL-48 | Done | Applicant intake draft and submission |
| TAL-49 | Done | Foundation/Auth Workspace Rebaseline |
| TAL-50 | Done | Current Worktree Intake and Backend Salvage Ledger |
| TAL-51 | Done | Clean MVP ERD and Schema Contract |
| TAL-52 | Done | Clean MVP Migration Baseline |
| TAL-53 | Done | Backend Schema Reconciliation Inventory |
| TAL-54 | Done | Backend Boot and Filament Registration Stabilization |
| TAL-55 | Done | Academic, Course, and Curriculum Foundation Adaptation |
| TAL-56 | Done | Admissions-to-Student Master Backend Adaptation |
| TAL-57 | Done | Student Panel Filament v5 Profile Boot Stabilization |
| TAL-58 | Done | Term Offerings and Resource Foundation Backend Adaptation |
| TAL-59 | Done | Registrar Term Offering Builder |
| TAL-60 | Done | Scheduling UI Baseline Realignment |
| TAL-61 | Done | Scheduling Demand Source Contract and Filament Readiness Surface |
| TAL-62 | Done | Solver Run Dispatch and Candidate Schedule Review |
| TAL-63 | Done | Cloud Run Solver Demand Contract Alignment |
| TAL-64 | Done | Cloud Run Solver Deployment Path Review |
| TAL-65 | Done | Laravel Cloud Run Solver Integration Verification |
| TAL-66 | Done | Candidate Schedule Approval and Publication |
| TAL-67 | Done | Enrollment Gate and Seat Reservation |
| TAL-68 | Done | Finance Assessment and Ledger Foundation |
| TAL-69 | Done | PayMongo Payment Evidence and Ledger Posting |
| TAL-70 | Done | Authenticated COR Output and Holds Alignment |
| TAL-71 | Done | Finance Outputs and Student Hub Finance |
| TAL-72 | Done | Grades MVP |
| TAL-73 | Done | Progression and Student Lifecycle MVP |
| TAL-74 | Done | Graduation and Completion Review MVP |
| TAL-75 | Done | Reports, Audit, and Export MVP |
| TAL-76 | Done | Bootstrap Public Landing Page Adaptation |
| TAL-77 | Done | Calendar-Event Availability Alignment and Solver Mapping |
| TAL-78 | Done | Current dirty-work cleanup and scheduling/access verification |
| TAL-79 | Done | Orchestrator Protocol and Vertical Slice Workflow Codification |
| TAL-93G | Done locally; pending explicit Linear sync | Canonical Permission Seeding & Role-Access Enforcement (7th sub-slice of parent TAL-93; HIGH-RISK security/authorization). ROOT-CAUSE FIX: DatabaseSeeder seeded 7 roles + 0 permissions; every can()/canAny() in the 18 permission-based policies failed -> permission-gated Filament surfaces invisible to ALL roles. Extended DatabaseSeeder with 26 canonical permissions (PRD 2.3 action-level: 25 original + request-grade-corrections for Student discovered in GradeCorrectionService) assigned to the 7 roles via syncPermissions per the locked PRD 2.3.1-2.3.7 matrix (Registrar 14, Accounting 4, Faculty 4, Academic Head 6, Super Admin 1, Student 1, Applicant 0). Idempotent per laravel-permission-development skill. Super Admin scoped per PRD 2.3.7 — NO Gate::before blanket bypass (config surfaces are role-gated via UserPolicy/RolePolicy/SystemSettingPolicy etc). Zero policy/resource/service edits needed (dynamic getNavigationGroup() remaps already route shared resources into viewer's nav group). Tests: new RoleAccessMatrixTest (22 tests: per-role exact permission set on Role model + 14 resource canAccess positive/negative cases + 26-perm completeness/no-orphan guard); SchemaConformanceTest updated (0->26 permissions baseline + role_has_permissions>0 assertion); StaffNavigationVisibilityTest passed unchanged (8/8 with no group leaks). Verification: full suite 572 passed / 4912 assertions / 0 failed; Pint passed; Larastan [OK] against baseline (no regen, no app/ edits); git diff --check clean. Unblocks TAL-93I (depends on manage-admission-setup). |
| TAL-93H1 | Done locally; pending explicit Linear sync | RETIRE step 1 of parent TAL-93H (Scheduling/curriculum static-analysis fixes; all sub-slices are phantom-surface retires). Primary factual audit (table existence + AdminPanelProvider registration on test_tala_db) reframed TAL-93H: ALL its TAL-93E baseline errors come from dead/phantom pre-rescue surfaces, none is a live add-a-method bug. H1 = retired the UNWIRED dead manual-assignment path in SectionMeetingAssignmentService (prepareForCreate/normalizeAssignmentData/assertNoConflicts/deliveryGroupFor + orphaned helpers assertRequired/assertFacultyIsEligible/assertTermIsNotPublished/requiresRoom/stringValue + 6 unused imports) which queried a phantom section_meetings schema (term_id/room/faculty_id/section_delivery_group_id/subject_id columns that do not exist on the live solver-centric table) and had no production caller; KEPT the live, TAL77-tested assertRecurringBlocksAllow() + integerValue/timeValue. The real Manual Schedule Override (PRD 6.2.5/6.5) belongs to the live schedule_runs/candidate_schedule_rows/section_meetings design and is routed to the CP-SAT integration slice (TAL-94), not faked here. Also reverted an in-progress modalityRequiresRoom() addition once the audit proved its only caller (DeliveryPatternService) is itself phantom -> routed to TAL-93H3. Verification (primary, direct, test_tala_db): full suite 572 passed/4912 assertions/0 failed; Pint passed; Larastan baseline regenerated 327->319 errors (all 8 removals = SectionMeetingAssignmentService entries, 0 remaining); phpstan analyse [OK]; git diff --check clean. Next: TAL-93H2 (retire phantom Curriculum/CurriculumSubject cluster). |
| TAL-93H2 | Done locally; pending explicit Linear sync | RETIRE step 2 of parent TAL-93H (phantom-surface retire, like TAL-93F). Retired the superseded pre-rescue flat curriculum design (tables curriculums/curriculum_subjects/curriculum_readiness_scopes MISSING in test_tala_db, no creating migration; CurriculumResource UNREGISTERED in AdminPanelProvider; superseded by the live registered CurriculumVersion/CurriculumEntry design per PRD 04_academic_setup.md line 268 + modules 05/06/07/11/13; validated by the accepted TAL-82B benchmark). Orchestrated to one worker; independently verified by primary. Deleted 18 files: 3 models (Curriculum, CurriculumSubject, CurriculumReadinessScope), 8 Filament Resources/Curriculums (Resource + 4 Pages + CurriculumForm + CurriculumInfolist + CurriculumsTable), CurriculumScopeReadinessService, CurriculumSubjectObserver, CurriculumPolicy + CurriculumSubjectPolicy, 3 factories. Unwired 2 live files: AppServiceProvider (removed the CurriculumSubject import + observer import + the CurriculumSubject::observe() call; CurriculumVersion + DeliveryPattern imports correctly retained) and Subject.php (removed the dead curriculumSubjects() hasMany, no live caller). Corrected 2 test references (no test weakened): RoleAccessMatrixTest repointed the TAL-93G phantom ref CurriculumResource -> live CurriculumVersionResource (academic-head canAccess=true, proven by TAL-82B); TAL82BCurriculumCourseCatalogAcceptanceTest assertNotContains now uses a single-quoted FQCN string literal so the anti-reintroduction guard survives the class deletion. KEPT untouched: live CurriculumVersion/CurriculumEntry + resource/policy/factories, CurriculumImportService/CurriculumImportTemplate (TAL-82D), AdminPanelProvider resources array; no DB table/column/migration changed. Verification (primary, direct, test_tala_db): full suite 572 passed / 4912 assertions / 0 failed; Pint passed; TAL-93E baseline regenerated 319->271 (all removed entries map to the deleted phantom files; non-phantom-removed-path check EMPTY); phpstan analyse [OK]; route:list JSON count 172 unchanged with live curriculum-versions routes intact; git diff --check clean. Next: TAL-93H3 (retire the phantom DeliveryPattern cluster). |
| TAL-93H3 | Done locally; pending explicit Linear sync | RETIRE step 3 (final) of parent TAL-93H - parent TAL-93H now COMPLETE (H1+H2+H3). Retired the abandoned DeliveryPattern delivery-template cluster (table delivery_patterns MISSING in test_tala_db, no creating migration; DeliveryPatternResource UNREGISTERED in AdminPanelProvider; DeliveryPatternService already broken - it called the non-existent SectionDeliveryGroup::modalityRequiresRoom(); ZERO PRD mentions of DeliveryPattern across all 13 modules; superseded by the live TermOffering (owns actual modality + overrides per PRD 05 line 71) + SectionDeliveryGroup (modality + delivery_override) design validated by the done TAL-85A-D slices). Orchestrated to one worker; independently verified by primary. Deleted 12 files: DeliveryPattern model, DeliveryPatternService, 8 Filament Resources/DeliveryPatterns (Resource + 4 Pages + DeliveryPatternForm + DeliveryPatternInfolist + DeliveryPatternsTable), DeliveryPatternPolicy, DeliveryPatternFactory. Unwired 1 live file: AppServiceProvider (removed the DeliveryPattern import + DeliveryPatternPolicy import + the Gate::policy(DeliveryPattern::class, DeliveryPatternPolicy::class) line; all other Gate::policy lines + imports retained). No test edits (the TAL85A assertTableColumnDoesNotExist('deliveryPattern.code') guard stays green as-is). SectionDeliveryGroup::modalityRequiresRoom NOT added (its only caller was deleted). KEPT untouched: live SectionDeliveryGroup/TermOffering/SectionMeeting/SchedulingDemand + their resources/services/tests; AdminPanelProvider resources array; no DB table/column/migration changed. Verification (primary, direct, test_tala_db): full suite 572 passed / 4912 assertions / 0 failed; Pint passed; TAL-93E baseline regenerated 271->223 (all removed entries map to the deleted DeliveryPattern files incl. the modalityRequiresRoom undefined-method error; non-DeliveryPattern-removed-path check EMPTY); phpstan analyse [OK]; route:list JSON count 172 deterministic (route-neutral; zero delivery-pattern routes remain; live scheduling-demands/sections routes present); git diff --check clean. Parent TAL-93H (scheduling/curriculum phantom-surface retires) COMPLETE. Next: TAL-93I (admission requirement policy configuration surface, unblocked by TAL-93G). |
| TAL-93I | Done locally; pending explicit Linear sync | Admission Requirement Policy Configuration surface (final BUILD sub-slice of parent TAL-93; core MVP config per PRD 13.1.1 #6/#7/#8 + rules 6/7/8, 13.8, 3.1). Orchestrated to one worker; independently verified by primary. BUILT the staff-configurable Filament v5 resource on the LIVE state-based AdmissionRequirementPolicy (replaces the offering-based resource removed in TAL-93F3): 8 files (Resource + Pages List/Create/Edit/View + Schemas Form/Infolist + Tables Table) mirroring FeeRules; Editable Table + Record Form per PRD 13.8; ViewAction+EditAction only, no delete/bulk (audit-safe supersede-not-delete via Draft/Active/Superseded + effective dating). Registered in AdminPanelProvider (Registrar nav group). Additive-only: 5 static Options helpers on AdmissionRequirementPolicy sourced from ApplicantIntake + ChecklistItem constants (no vocab drift) + 3 EvidenceMethod constants on ChecklistItem. Authorization automatic via the kept AdmissionRequirementPolicyPolicy gating on manage-admission-setup (Registrar-only; Super Admin NO blanket bypass). Added idempotent AdmissionRequirementPolicySeeder (called from DatabaseSeeder; no-ops under testing; seedBaseline extracted for testability) creating a minimal ACTIVE baseline (IDENTITY_DOCUMENT digital-upload blocks-handover + one physical doc per the 3 natural category+credential pairs) so a fresh DB is submit-ready. Tests: TAL93IAdmissionRequirementPolicyConfigTest (13) + TAL93IAdmissionRequirementPolicySeederTest (2). Verification (primary, direct, test_tala_db): full suite 587 passed/5032 assertions/0 failed; route:list 176 (+4 admission-requirement-policies routes); phpstan-baseline UNCHANGED + focused Larastan OK; Pint clean; git diff --check clean. No schema/migration change; resolver/ApplicantIntakeService logic untouched. Parent TAL-93: only TAL-93J (the gate) remains. |
| TAL-93J1 | Done locally; pending explicit Linear sync | Gate Environment and Dependency Preparation (tooling/security; no product behavior). Worker and independent primary acceptance established forced PHPUnit testing/MySQL/`test_tala_db` targeting plus an early `Tests\\TestCase` guard, while preserving local/MySQL/`tala_db` for development. `DatabaseSeeder` was proven idempotent on development data (7 roles, 26 permissions, 30 role-permission assignments, 6 admission policies, 2 users). Retired unused `crash-mcp` and `xlsx`; authority wording now reflects native CSV handling. Follow-up compatible patches updated Vite, concurrently, shell-quote, form-data, and hasown. Final verification: full `npm audit` 0 vulnerabilities; `npm run build`, Pint, full PHPStan, Composer audit, and diff check clean; full PHPUnit 587 passed / 5,032 assertions on `test_tala_db`. No product behavior, schema, migrations, `.env`, global tooling, J2/J3, Linear, or external state changed. Next: plan TAL-93J2 read-only Ground-Truth/static-analysis audit. |
| TAL-93J2 | Done locally; pending explicit Linear sync | Whole-Repository Ground-Truth & Static-Analysis Audit (read-only discovery; parent TAL-93). NO product code changed. Method: inventory (55 Filament resources vs 38 registered in AdminPanelProvider; ~76 models vs migration-created tables) -> 3 parallel read-only domain workers (faculty/scheduling, grades/finance, admissions/catalog), all PASS -> primary independently re-verified via laravel-boost `database-query` MCP on test_tala_db (all 18 candidate tables ABSENT; all 12 live-replacement tables PRESENT) + PRD citations (05 §5.2 line 88, 04, 03, 07, 08.4, 10.2/10.4/10.5, 02/12). Findings: ~15 SUPERSEDED-REMNANT phantom surfaces (missing table + unregistered resource + only phantom/no-live callers, each with a live tabled+tested replacement) -> retire; FaqEntry REQUIRED-BUT-UNBUILT (live caller StudentDashboardService::help() + `manage-faqs` seeded + PRD 02/12, but `faq_entries` table missing) -> build; CorVerification deferred scaffold (TAL-88C) -> user decided RETIRE; PromissoryNote CONTESTED (PRD §8.7 required concept, no `promissory_notes` table, full PromissoryNoteLifecycleService + `approve-promissory-notes` permission, wired live into PaymentConfirmationService + PayMongoWebhookProcessor as a Schema::hasTable-guarded no-op) -> diagnose-first + user decision. Two primary corrections to worker output: (1) SubjectSuggestionService is LIVE/tested (TAL72GradesMvpTest) and excluded from the Subject retire; (2) PromissoryNote downgraded from "retire" to "contested/decide". Routed to separately-approved fix boundaries TAL-93J2a-g (see Next-Steps map). Independent verification: git worktree clean; the extra HEAD commit 35b368d is the user's own protocol amendment (benign). No product/schema/migration/.env/Linear/push change. Next: TAL-93J2a (faculty-availability retire). |
| TAL-93J2a | Done locally; pending explicit Linear sync | Retire faculty-availability phantom island + FacultySubjectEligibility family + standalone SectionDeliveryGroupResource (first fix boundary of TAL-93J2; superseded-remnant retires). Orchestrated to one worker; independently verified by primary + a cold independent auditor (verdict JUSTIFIED). DELETED 52 files: 35 faculty-availability (4 models + 2 services in app/Actions/Scheduling + 3 unregistered resource dirs + 3 policies + 4 factories); 9 FacultySubjectEligibility (resource dir + model + policy + factory); 8 standalone app/Filament/Resources/SectionDeliveryGroups resource dir. KEPT the LIVE replacements: CalendarEvent/calendar_events (PRD 05 §5.2 line 88 "stored as calendar_events rather than a submission/locking/version/change-request workflow"; corroborated by PRD 06:195 "solver does not read legacy faculty-availability period/submission/window/change-request records"), FacultyQualification/faculty_qualifications (PRD 05 §5.2 flat mappings), and DeliveryGroupsRelationManager under SectionResource + the SectionDeliveryGroup model/table/policy/factory. MODIFIED 7 files: removed the dead facultySubjectEligibilities() hasMany from User (live) + Subject; DatabaseSeeder retired 4 orphaned permissions (manage-faculty-subject-eligibilities, review-lock-faculty-availability, submit-faculty-availability, view-faculty-availability) from canonicalPermissions + registrar/academic-head/faculty maps and added an idempotent prune of non-canonical permissions (plus the existing cache flush); RoleAccessMatrixTest completeness 26→22 + trimmed role sets; SchemaConformanceTest 22 permissions / 23 assignments; StaffNavigationVisibilityTest repointed to assert stale perms cannot unlock CalendarEventResource/FacultyQualificationResource (CorVerification assertion kept for TAL-93J2g; 4 slugs added to the absence guard); regenerated phpstan-baseline.neon (pure removals: 288 lines / 48 entries, 0 insertions, all mapping to deleted files). NO PRD change (PRD §2.3 names capabilities, not the retired slugs; grep=0). Verification (primary, direct, test_tala_db): full suite 587 passed / 5037 assertions / 0 failed; PHPStan [OK]; Pint clean; route:list 176; zero dangling references; git diff --check clean. Dev tala_db reconciled via idempotent non-destructive DatabaseSeeder (proven target APP_ENV=local / DB=tala_db): 7 roles / 22 permissions / 23 assignments / 2 users / 0 retired slugs (was 26/30 with 4 present). No migration/schema/.env/JS change; no push; no Linear. Next: TAL-93J2b (grades phantom cluster). |
| TAL-93J2b | Done locally; pending explicit Linear sync | Retire the DEAD "grades-table" design (second fix boundary of TAL-93J2; superseded-remnant retire, live replacement = the grade_rosters design per PRD 10). Orchestrated to one worker (returned a clean, all-clear Ground-Truth re-verification but was budget-blocked before implementation, making zero changes); primary then completed implementation + independent verification + a cold independent auditor (verdict JUSTIFIED). GTG evidence (Boost database-query on test_tala_db + tala_db): the 4 grade-table-design tables (grades/grade_corrections/grade_submission_packages/grade_submission_package_items) are ABSENT in both DBs with NO Schema::create migration; the 3 Filament resources were UNREGISTERED in AdminPanelProvider; every deleted class had only dead callers. DELETED 36 files: 4 models (Grade/GradeCorrection/GradeSubmissionPackage/GradeSubmissionPackageItem); 7 dead services/DTOs in app/Actions/Grades (GradeEncodingService/GradeSubmissionPackageService/GradeCorrectionService/GradeCorrectionResult/GradeFinalizationService/GradeFinalizationResult/CollegeGradingService); 3 policies (GradePolicy/GradeCorrectionPolicy/GradeSubmissionPackagePolicy); 4 factories; 1 enum (GradeCorrectionStatus); 2 exceptions (InvalidGradeException/MissingGradePeriodException); 15 unregistered Filament resource files (Grades/GradeCorrections/GradeSubmissionPackages dirs). UNWIRED dead grade edges (edits, keeping the files): Enrollment::grades(); Subject::grades()+gradeCorrections()+HasMany import; EnrollmentSubject::grade()+canReceiveFacultyGrade()+HasOne import; EnrollmentSubjectPolicy grade methods + view-grade-submission-progress in viewAny/view; EnrollmentSubjects Table/Infolist/Resource grade columns/actions/filter/eager-load + view-grade-submission-progress branch (kept the class-list+finance shell for J2c); StudentDashboardService requests()/gradeCorrections() dead []-projection + PHPDoc shape. KEPT the LIVE grade_rosters design intact (GradeRoster/GradeRosterRow/GradeOutcomeEvent/LateGradeAuthorization + GradeRosterResource/RowsRelationManager + FacultyGradeRoster page + student GradesView + 10 live actions incl. GradePolicyService/GradeWindowService/RecordApprovedGradeCorrection/GenerateGradeRoster/PostAndReleaseGradeRoster; OperationalReportService still uses GradeOutcomeEvent). RECONCILED 6 orphaned permissions (encode-grades/finalize-grades/verify-grade-submissions/manage-grade-corrections/request-grade-corrections/view-grade-submission-progress) in DatabaseSeeder + RoleAccessMatrixTest + SchemaConformanceTest + StaffNavigationVisibilityTest -> 16 canonical permissions / 17 assignments (Student -> 0 action perms; faculty -> view-class-list only). NO PRD change (6 slugs absent from all PRD .md; grade capabilities remain via the role-gated GradeRoster workflow; student in-TALA correction request out-of-MVP per PRD 2.3.2/10.4). AppServiceProvider NOT touched (it registers no grade policy; they were convention-auto-discovered and die with their models). Verification (primary, direct, test_tala_db): full suite 587 passed / 5043 assertions / 0 failed; focused 132 passed / 1074 assertions; PHPStan [OK] No errors; phpstan-baseline.neon regenerated as pure removals (118->51 entries, 402 deletions / 0 insertions); route:list 176 unchanged; Pint clean (fixed StudentDashboardService only); git diff --check clean; zero dangling references to any deleted class. Dev tala_db reconciled via idempotent DatabaseSeeder (proven target APP_ENV=local / DB=tala_db): 7 roles / 16 permissions / 17 assignments / 0 retired grade slugs. No schema/migration/.env/push/Linear. Next: TAL-93J2c (catalog/enrollment/docs phantom cluster). |
| TAL-93J2c | Done locally; pending explicit Linear sync | Retire the catalog/enrollment/docs phantom cluster (third fix boundary of TAL-93J2; superseded-remnant retire). Executed primary-direct under `Primary proceed` + independent primary verification + cold independent auditor (verdict JUSTIFIED). GTG (Boost database-query on test_tala_db + tala_db): subjects/enrollment_subjects/document_uploads ABSENT in both DBs, no Schema::create migration, the 3 resources UNREGISTERED in AdminPanelProvider, no live caller; no live-model ::class edge into any phantom model. DELETED 28 files: (1) Subject cluster (11) = Subject model + Subjects resource dir (8) + SubjectPolicy + SubjectFactory -> live Course + CourseSpecification (PRD 04; TAL82B benchmark); (2) EnrollmentSubject cluster (9) = EnrollmentSubject model + EnrollmentSubjects resource dir (5) + EnrollmentSubjectPolicy + ORPHANED FacultyClassListService + FacultyClassListRow (only caller = deleted EnrollmentSubjectsTable; forSectionSubject queried absent enrollment_subjects/section_teacher) -> live CourseEnrollment/course_enrollments (PRD 07 term-offering/section binding); (3) DocumentUpload cluster (8) = DocumentUpload model + DocumentUploads resource dir (5) + DocumentUploadPolicy + ORPHANED DocumentUploadReviewService (only caller = deleted DocumentUploadsTable; operated on absent document_uploads; superseded by the LIVE ChecklistItemsRelationManager under StudentProfileResource) -> live DocumentEvidence/document_evidence + ChecklistItem (PRD 03 flat checklist). Removed the now-empty app/Actions/Faculty dir. KEPT (excluded): SubjectSuggestionService (imports StudentProfile, not Subject; tested by TAL72GradesMvpTest). PERMISSION prune (Option A, user-decided): view-class-list was vestigial (its only checkers EnrollmentSubjectPolicy/FacultyClassListService/EnrollmentSubjectResource all deleted; live faculty class list = role-gated FacultyGradeRoster page, PRD 10.5/01#30) -> removed from DatabaseSeeder -> 15 canonical permissions / 16 assignments, faculty -> 0 action permissions. NO PRD change (capability via role-gated FacultyGradeRoster; slug grep in PRD = 0). EDITS (6): DatabaseSeeder (prune view-class-list, faculty->[]); RoleAccessMatrixTest (faculty resource case EnrollmentSubjectResource -> FacultyGradeRoster::class [role-gated, passes with 0 perms]; faculty expected -> []; completeness 16->15); SchemaConformanceTest (15/16); StaffNavigationVisibilityTest (+view-class-list absence guard); TAL82BCurriculumCourseCatalogAcceptanceTest (assertNotContains(SubjectResource::class) -> single-quoted FQCN string literal + dropped import, so the anti-reintroduction guard survives deletion); phpstan-baseline regenerated. AppServiceProvider NOT touched (registers none of the 3 policies; convention-discovered). Verification (primary, direct, test_tala_db): full suite 587 passed / 5044 assertions / 0 failed; focused 102 passed / 607 assertions; PHPStan [OK]; baseline pure removals (72->61 entries, 54 deletions / 0 insertions); route:list 176 unchanged; Pint clean (ordered_imports on RoleAccessMatrixTest); git diff --check clean; zero real dangling references (only the intended TAL82B string literal). Dev tala_db reconciled via idempotent DatabaseSeeder (proven APP_ENV=local / DB=tala_db): 7 roles / 15 permissions / 16 assignments / faculty 0 / view-class-list absent. No schema/migration/.env/push/Linear. FOLLOW-UP (auditor-caught, primary-confirmed): manage-curricula is now assigned (registrar+academic-head) but checked by no live policy — its sole checker SubjectPolicy gated the unregistered phantom SubjectResource and was already dead — routed to TAL-93J3 (wire into live curriculum resource policies vs formally accept role-gating); NOT pruned in J2c (real PRD 2.3.4/2.3.6 governance capability, out of approved scope, material change to 2 roles). Next: TAL-93J2d (finance phantom cluster). |
| TAL-93J2d | Done locally; pending explicit Linear sync | Retire the finance phantom cluster (fourth fix boundary of TAL-93J2; superseded-remnant retire). Executed primary-direct under `Primary proceed` + independent primary verification + cold independent auditor (verdict: all 7 checks PASS). Before execution the primary merged a second agent's plan after independently re-verifying via Serena that it caught 3 real oversights the initial 28/0 plan had missed (orphaned InstallmentPolicyService, orphaned HasAccountingConfigurationScope trait, and a TAL-68 test reference) -> final scope 30 deletions + 3 edits. GTG (Boost `database-query` on test_tala_db + tala_db + Serena `find_referencing_symbols`): fee_templates/installment_policies/installment_policy_milestones ABSENT in both DBs, no `Schema::create` migration, the 3 resources NOT in the AdminPanelProvider explicit `->resources()` list (unregistered -> zero routes), InstallmentPolicyService had ZERO referencing symbols, HasAccountingConfigurationScope referenced only by the two deleted models. DELETED 30 files: 3 models (FeeTemplate/InstallmentPolicy/InstallmentPolicyMilestone) + 21 Filament resource files (FeeTemplates dir 8, InstallmentPolicies dir 8, InstallmentPolicyMilestones dir 5) + 3 policies (FeeTemplatePolicy/InstallmentPolicyPolicy/InstallmentPolicyMilestonePolicy) + FeeTemplateFactory + ORPHANED InstallmentPolicyService (app/Actions/Finance; queried the absent installment tables via DB::table, superseded by the live Assessment/LedgerEntry/PaymentScheduleRow flow + EnrollmentFinanceClearanceService) + ORPHANED HasAccountingConfigurationScope trait (app/Models/Concerns). KEPT the LIVE replacements intact: FeeRule/`fee_rules` + AssessmentLine (PRD 08.4); PaymentScheduleRow/`payment_schedule_rows` + FinancialAccommodation/`financial_accommodations` (PRD 08 §8.5.1/§8.7). MODIFIED 3 files: TAL68FinanceAssessmentLedgerTest.php (dropped the FeeTemplateResource import; KEPT the FeeRuleResource::shouldRegisterNavigation()=true assertion; converted the FeeTemplateResource assertion to a class-absence string-literal guard `assertFalse(class_exists('App\Filament\Resources\FeeTemplates\FeeTemplateResource'))` per the TAL82B/J2c anti-reintroduction precedent; renamed the test method to match); phpstan-baseline.neon (regenerated, pure removals); TALA-Rescue-Next-Steps.md (tracker record). AppServiceProvider NOT touched (registers none of the 3 policies; convention-discovered, they die with their models). NO permission/seeder change (create-assessments/process-payments/approve-promissory-notes remain live-checked by EnrollmentPolicy/PromissoryNotePolicy/PromissoryNoteLifecycleService -> no orphan; seeders untouched -> NO dev-DB reseed needed). NO PRD/blueprint/architecture change (no .md references FeeTemplate/InstallmentPolicy). NO migration/schema/.env. Benchmark: reused the accepted TAL-68/71/86D/86E finance + TAL-93F/H/J2a-c phantom-retire benchmark + PRD 08 authority; Academico qualified-reference = fee definitions + concrete scheduled payments attached to enrollments, NOT a separate installment-policy template engine (confirms the retirement direction only; no Academico code adopted). Verification (primary, direct, test_tala_db): full suite 587 passed / 5044 assertions / 0 failed (a first background run hit 3 transient MySQL role-seed deadlocks caused by a force-stopped test process; a clean fresh run was 587 green); TAL68 alone 14 passed / 134 assertions; RoleAccessMatrixTest alone 22 passed; PHPStan [OK]; baseline pure removals 42->32 entries / 60 deletions / 0 insertions; route-neutral at 175 (verified via pre/post git stash; the earlier "176" was a text-line-count artifact, not a real target); zero dangling refs (only the intended TAL68 string literal); Pint clean; git diff --check clean; cold independent auditor all 7 checks PASS. EXCLUDED PromissoryNote/PromissoryNotePolicy/PromissoryNoteLifecycleService (TAL-93J2f, contested). No push; no Linear. Next: TAL-93J2e (BUILD FaqEntry). |
| TAL-93J2e | Done locally; pending explicit Linear sync | COMPLETE/PATCH the partially-built admin-curated FAQ so the PUBLIC LANDING FAQ is dynamic/admin-managed (user decision 2026-07-10 = dynamic add/edit/delete, not fixed at 3). Fifth fix boundary of TAL-93J2; executed via `Orchestrate` (one accountable worker) + independent primary verification + Playwright live smoke. GTG-corrected from the stale "BUILD from scratch" framing after primary verified (Boost `database-query` + code reads) that the model, FaqEntryPolicy, the 7-file Filament resource scaffold, `manage-faqs` (super-admin only), RBAC tests, and a 3-item static landing accordion were ALREADY built; the true gaps were the missing `faq_entries` table (Boost confirmed absent both DBs, no migration), the unregistered resource, no factory/seeder, and static (non-dynamic) landing content. Merged a second agent's plan after independently verifying its real catches (FilamentRegistrationStabilizationTest asserts the faq-entries route ABSENT; `help()`/`forStudent()` is dormant with no live caller so no Student Hub FAQ page per PRD 12; `system_key` idempotent seeding) and correcting its over-reach (Playwright was not installed then; Spatie activitylog IS installed; delete was not wired). BUILT: `faq_entries` migration (question string, answer text, category default general, sort_order unsignedInteger default 0, is_published bool default false, nullable unique system_key, created_by/updated_by foreignId nullable constrained('users') nullOnDelete, index (is_published,sort_order), timestamps); FaqEntryFactory (+published() state); idempotent FaqEntrySeeder (firstOrCreate on system_key for the 3 baseline landing questions, activity logging suppressed for system baseline content). MODIFIED: registered FaqEntryResource in AdminPanelProvider (System Administration group); added Filament\Actions\DeleteAction to FaqEntriesTable; FaqEntry model +HasFactory +Spatie LogsActivity (mirrors CalendarEvent: useLogName('faq'), logOnly content fields, logOnlyDirty, dontSubmitEmptyLogs) +scopePublishedOrdered +system_key fillable (kept consts/hooks/relations/category helpers); routes/web.php `/` passes FaqEntry::query()->publishedOrdered()->get() to welcome; welcome.blade.php replaced the 3 hardcoded #faqAccordion items with an ESCAPED @forelse loop (unique collapseFaq{id}/headingFaq{id}, {{ }} question/answer, preserved wrapper/classes/card-blur/data-bs-parent/nav anchor) + muted empty-state (inline Apply link in Q1 intentionally dropped to plain text per plan); DatabaseSeeder calls FaqEntrySeeder (permission seeding untouched). TEST REPOINTS/ADDS: FilamentRegistrationStabilizationTest moved 'faq entries' from deferredStaleAdminRoutes() to retainedPanelAndFoundationRoutes(); SchemaConformanceTest added faq_entries to APPLICATION_TABLES (59->60, counts 77->78); new TAL93J2eFaqEntryAcceptanceTest (8 tests: schema/index/FK/unique, idempotent non-overwriting seeding, super-admin CRUD incl delete + audit attribution, RBAC denial, validation/category restriction, published-only + deterministic public render, unpublished + unsafe-HTML not rendered, empty state). DOC UPDATES: PRD 02 §2.2 item 1 (landing FAQ is dynamic admin-curated published FaqEntry) + §2.1.7 (FAQ/public-content curation added to System Super Admin scope); PRD 12 §12.1 (FAQ is landing-only, explicitly no Student Hub FAQ page); ui_surface_blueprint.md (`/` FAQ dynamic admin-managed + FaqEntryResource registered); architecture_specification.md (`/` Blade reads published FaqEntry). REGRESSION FIXED at root cause: adding LogsActivity + the non-transactional SchemaConformanceTest committing 3 seeded audit rows broke TAL92ERetentionDisposalTest's absolute Activity::count() -> suppressed activity logging inside the baseline seeder (system content has no causer). VERIFICATION (independent primary): full suite 595 passed / 5100 assertions / 0 failed on test_tala_db; PHPStan [OK]; phpstan-baseline pure removals 0 insertions / 12 deletions (dropped now-resolvable FaqEntry created_by/updated_by ignores, NOT a silent regenerate); route:list 178 (175 + 3 new faq-entries admin routes; retired phantom routes still absent); Pint clean; git diff --check clean (benign CRLF-normalize note on architecture_specification.md); Playwright desktop+mobile landing smoke PASS (3 seeded questions render in DB order, accordion aria-expanded false->true on click, 0 console errors). Dev tala_db reconciled (migrate + idempotent seed proven on APP_ENV=local/DB=tala_db = table + 3 published baseline entries, no duplicates). No cold auditor (additive build, not a deletion set). Playwright: user-activated; primary ran it as a supplementary non-blocking smoke, PHPUnit remained the acceptance gate. EXCLUDED and untouched: StudentDashboardService::help()/forStudent() (dormant), Student Hub/Applicant FAQ page, rich-text/attachments/search/analytics, package/permission changes, J2f/J2g. No push; no Linear. Next: TAL-93J2f (PromissoryNote decision, diagnose-first). |
