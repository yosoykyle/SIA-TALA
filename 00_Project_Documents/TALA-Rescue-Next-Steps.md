# TALA Rescue Next Steps

## Purpose

This is the active planning surface: it controls issue order and scope, not product behavior. It is read after `AGENTS.md` and `TALA-Orchestrator-Protocol.md` in the intake chain. For each issue it shows only the status, goal, sub-slice map (when split), dependency lock, and next boundary. The protocol owns the full cycle — planning, gates, delegation, verification, tracker movement, commits, and sync — so do not restate those here.

- **Issue numbering:** the next issue continues from the last ID in `TALA-Local-Linear-Sync-Tracker.md` (or Linear).
- **Sub-slice maps:** when a parent issue is split, the primary records the map here on plan acceptance (ID, one-line purpose, status, and next boundary per sub-slice) and keeps the parent here until every sub-slice is complete; completed sub-slices are recorded in the tracker.
- **Resume:** after compaction, interruption, rejected worker output, or stale state, run the resume checkpoint from the protocol before continuing.

## Active and Upcoming Issues

Dependency lock:

1. Identity, roles, panels, and base administration come first.
2. Academic setup and calendar come before admissions handover because handover assigns Program and Curriculum.
3. Holds and lifecycle foundation come before enrollment because gates and COR visibility depend on student state.
4. Term offerings, resources, and a published Master Schedule come before enrollment binding.
5. Finance core comes before official enrollment because assessment, ledger, downpayment, accommodation, and Finance Gate affect enrollment.
6. Student Hub comes after source records exist; it is a projection, not a source module.
7. A pre-integration regression gate (TAL-93) must pass before integration hardening begins, proving the SIS foundation is clean.
8. CP-SAT and PayMongo end-to-end hardening (TAL-94/95) start only after the pre-integration gate passes; they are human-gated and require credentials/deployment steps.
9. A post-integration regression gate (TAL-96) runs after both integrations are wired in, catching regressions introduced by external service handlers before demo preparation.
10. Demo and rehearsal support (TAL-97) builds only on a fully verified and integration-tested system.

| Issue | Status | Goal |
| --- | --- | --- |
| TAL-91 | Done locally; pending explicit Linear sync | Student Hub Projection Acceptance: validate student-safe views for enrollment, schedule, finance, COR/output, grades, holds, lifecycle, completion, and notices. Split into TAL-91A-D, all complete; see Local Linear Sync Tracker. |
| TAL-92 | Done locally; pending explicit Linear sync | Reports, Audit, Imports, Retention, and Remaining Admin Acceptance: validate fixed reports/exports, audit evidence, guarded imports, retention categories, integration settings, operational monitoring, and remaining system configuration. Owning contract PRD `13_system_admin_reports_audit.md`. Split into TAL-92A–F, all six complete; see Local Linear Sync Tracker. |
| TAL-93 | Planned - split into TAL-93A-I + TAL-93J1-J3 (J2 read-only audit complete -> fix boundaries TAL-93J2a-g) | Cross-Role Regression, Security, and UAT Readiness (Pre-Integration Gate). Sub-slices TAL-93A-I and TAL-93J1 are done locally; the TAL-93J2 read-only Ground-Truth audit is complete and its findings are routed to separately-approved fix boundaries TAL-93J2a-g (see map below), which resolve before TAL-93J3 (final automated cross-role/security/UAT-readiness gate). TAL-93J3 must pass before integration hardening (TAL-94/95) begins. |
| TAL-94 | Planned | Build-state UNVERIFIED — do NOT assume "hardening" means it works: this slice MUST start with (1) a Ground-Truth Gate on the integration (tables? real solver endpoint vs stub? passing tests? deployed/credentialed?) classifying each surface aligned/gap/phantom, and (2) the MANDATORY Benchmark Gate (mature CP-SAT/scheduling practice + Academico reference + CHED loading/scheduling policy) to validate the PRD design is realistic; if the PRD design is wrong, correct it via the Authority Document Correction rule and scope the fix to patch/simplify/bounded-rebuild-of-that-surface only (never a system restart), deferring disproportionate richer behavior to a post-MVP issue. CP-SAT End-to-End Scheduling Hardening: prove validated foundation records through solver dispatch, candidate review, publication, schedule visibility, and safe failure/infeasibility handling. Human-gated; requires credentials, solver deployment, and manual verification steps. Deferred items routed here: from TAL-92B — add audit logging (`activity_log`) for solver run records (PRD §13.6 scope 8's solver-run half) once solver dispatch is wired in; from TAL-92A — Solver Run History report (PRD §13.3.4 admin/audit report #6); from TAL-92F: wire the Schedule Released production notification trigger once schedule publication is live. |
| TAL-95 | Planned | Build-state UNVERIFIED — do NOT assume "hardening" means it works: this slice MUST start with (1) a Ground-Truth Gate on the integration (live payment code EXISTS — PaymentConfirmationService/PayMongoWebhookProcessor — but real gateway wiring / webhook verification / idempotent posting are unverified: real gateway vs stub? passing tests? credentialed?) classifying each surface aligned/gap/phantom, and (2) the MANDATORY Benchmark Gate (mature e-payment/gateway+ledger practice + Academico reference + RA 10173 + BSP/e-payment regs) to validate the PRD design is realistic; if the PRD design is wrong, correct it via the Authority Document Correction rule and scope the fix to patch/simplify/bounded-rebuild-of-that-surface only (never a system restart), deferring disproportionate richer behavior to a post-MVP issue. Payment Gateway End-to-End Hardening: prove payment attempt, gateway evidence, webhook verification, idempotent ledger posting, Finance Gate, and Accounting/Student visibility. Human-gated; requires API keys, webhook endpoints, and test-mode payment verification. Deferred items routed here: from TAL-92B — add audit logging (`activity_log`) for PayMongo checkout-attempt creation (PRD §13.6 scope 5's PayMongo half) once the live gateway is wired in; from TAL-92A — PayMongo Webhook Event Log report (PRD §13.3.4 admin/audit report #7); from TAL-92F: wire the Payment Received production notification trigger once the live gateway is wired in. |
| TAL-96 | Planned | Cross-Role Regression and Integration Coherence (Post-Integration Gate): verify the full system remains correct after CP-SAT and PayMongo integrations are wired in; catch regressions introduced by external service handlers, solver publication effects, and payment posting side-effects. |
| TAL-97 | Planned | Demo and Rehearsal Support from Verified MVP: rebuild only the realistic demonstration support needed for accepted flows, on top of a fully verified and integration-tested system. |
| TAL-98 | Planned (future enhancement, post-MVP) | Archival & Offline-Storage Management: optional archive-management interface (browse/restore archived and exported records), automated offline/cold-storage export, and on-premise HDD/SSD backup/replication target. Deferred from the TAL-92E retention clarification (PRD §13.7.5 point 7): V1 keeps all records in the operational database via soft-archive (hidden-but-queryable), so this is not required for the capstone MVP. Also the natural home for automated disposal jobs (PRD §13.7.4 rule 10) if the institution later explicitly requires them. Not a dependency for any MVP slice. |
| TAL-99 | Planned (future enhancement, post-MVP) | Data-Subject Privacy Request Handling & Log (RA 10173 §16): intake, DPO triage/fulfilment, and logging of data-subject requests (access, rectification, erasure/blocking, object, data portability, complaint). Deferred from TAL-92F: PRD §13.3.4 admin/audit report #10 (Privacy Request Log) has no source record, and RA 10173 with its IRR mandate no specific in-system request-log UI — data-subject-request handling is a DPO-owned manual/hybrid process. No MVP slice depends on it; access-request evidence is already partly served by `activity_log` + `output_access_logs`, and the erasure/blocking right routes through the TAL-92E hold-aware disposal-review ledger + the TAL-98 archival scope. Not a dependency for any MVP slice. |
| TAL-100 | Planned (future enhancement, post-MVP) | Configurable Notification Templates: admin-editable, database-backed email/notification templates (subject + body per notification type), replacing code-defined content. Deferred from TAL-92F: PRD §13.1.1 configurable record #17. V1 defines notification content in code (Laravel Mailable classes + Blade views); DB-editable templates are a post-MVP administration convenience, not an MVP dependency — no MVP slice requires them. |
| TAL-101 | Planned (future enhancement, post-MVP) | Database-Level Audit Tamper-Evidence Hardening: append-only / write-once protection for the `activity_log` table (e.g., MySQL triggers blocking UPDATE/DELETE, or hash-chaining for cryptographic verifiability). Deferred from TAL-93A (PRD 13.6 note). V1 enforces audit immutability at the application layer only (read-only `ActivityPolicy`, `canCreate()=false`), which the TAL-93A benchmark found proportionate for the capstone MVP; DB-level tamper-evidence is a post-MVP hardening enhancement. Not a dependency for any MVP slice. |

### TAL-93 Sub-Slice Map

Parent TAL-93 (pre-integration gate) is split into twelve top-level sub-slices (recorded 2026-07-08; re-sequenced after discovered defects; remaining boundary expanded to TAL-93J1-J3 by delegated primary decision on 2026-07-10). TAL-93A-I are DONE locally. Their work repaired test isolation and authorization, established the Larastan baseline, retired phantom pre-rescue surfaces, and rebuilt the live Admission Requirement Policy configuration surface. The original TAL-93J wording no longer describes the current gate accurately: PRD 13 contains role-owned Registrar, Accounting, Faculty, Academic Head, and System Super Admin surfaces, not a blanket Super-Admin-only boundary. The 93F/93H lesson also proves that passing tests do not detect unreachable phantom code: static analysis identifies suspicious code, the Ground-Truth Gate classifies it, and tests verify that any later correction is safe. TAL-93J1 therefore prepares the environment, TAL-93J2 performs discovery/classification without product changes, and TAL-93J3 runs the final automated cross-role/security gate. Findings from TAL-93J2 that require changes become separately approved fix boundaries before TAL-93J3.

| Sub-slice | Nature | Purpose | Status |
| --- | --- | --- | --- |
| TAL-93A | Housekeeping (decision + cleanup) | DONE. (a) Audit-immutability accepted as application-layer for V1 (proportionate per benchmark; the money trail in `ledger_entries` is separately protected by TAL-86C's reversal-ledger), DB-level tamper-evidence routed to post-MVP TAL-101 and the PRD 13.6 note updated. (b) Removed unused `pxlrbt/filament-activity-log`. (c) Removed unused `maatwebsite/excel` and deleted `config/excel.php`; both reconciled in the architecture spec. Removal proven safe (relevant tests 36/440 + isolated 4/4, composer validate, route:list 123, no dangling refs, Pint, diff-check; no app/ PHP changed). Two pre-existing whole-project debts surfaced and routed to TAL-93B (test-isolation, done) and TAL-93E (static-analysis baseline). | Done locally; pending explicit Linear sync |
| TAL-93B | Test-isolation repair (tool-fix) - DONE | DONE (independently verified by primary). Made role-seeding idempotent (`Role::create(['name'=>$x])` -> `Role::findOrCreate($x,'web')`) in 4 test files (StudentHubTest, TAL72GradesFilamentTest, TAL72GradesMvpTest, TAL82AcademicSetupAcceptanceTest), matching the suite convention + laravel-permission-development skill + Boost Laravel 12 docs (RefreshDatabase only transaction-wraps an already-migrated DB, so seeded roles persist and non-idempotent create() collides). Full suite 30 failed -> 9 failed (544 passed, 4859 assertions), 0 RoleAlreadyExists, no regressions. UNMASKED two pre-existing GENUINE failures (fail in isolation), routed to TAL-93C and TAL-93D. | Done locally; pending explicit Linear sync |
| TAL-93C | Feature fix — DONE | DONE. Retired the non-functional legacy PersonalDataCorrectionRequest feature (contradicts PRD §3.5; no migration/table existed; phantom lrn/birthdate fields; wrote to immutable intake; unspecified in PRD). Aligned to PRD §3.5: Registrar Admin-Override now edits all locked identity fields (name + DOB + prior_identifier/LRN); student self-service already existed. Added audit logging to both surfaces (§3.5 rule 2). Blueprint reconciled. New TAL93CProfileUpdateAlignmentTest (2 tests). Benchmark: RA 9048/10172 + DepEd LRN = K-12 optional for college = prior_identifier. Full suite 3 failed (TAL-93D auth only) / 546 passed / 0 skipped. |
| TAL-93D | Feature fix (diagnose-first) - DONE | DONE. Diagnosed the verified-user 403 regression as stale/underspecified student fixtures, not a product authz bug: Student Hub access is intentionally gated by an active official `StudentProfile` after handover. Added StudentProfile fixtures to the positive student cases in `Auth\EmailVerificationBoundaryTest` and `Auth\RoleAwareLoginLandingTest`; production auth code was unchanged. Full suite now green: 549 passed / 4879 assertions. | Done locally; pending explicit Linear sync |
| TAL-93E | Static-analysis baseline (tool-fix, small) - DONE | DONE. Generated `phpstan-baseline.neon` from the current whole-project Larastan level 5 inventory and included it from `phpstan.neon`, so existing debt is accepted and only new errors surface. Live pre-baseline inventory superseded the earlier snapshot: 393 file errors total, 333 `property.notFound`, 60 non-property. Non-property triage found no safe isolated PHP cleanup inside this tool slice; higher-signal items remain captured in the baseline rather than broadening into product fixes. Success proven: whole-project Larastan reports no errors against the baseline. Post-baseline review (during TAL-93F/G planning) reclassified ~14 of the 60 non-property errors as genuine latent bugs - undefined constants/methods on the concrete State* models (AdmissionRequirementPolicy, Section, SectionDeliveryGroup) and a large orphaned tableless AdmissionOffering cluster - routed to TAL-93F + TAL-93G for fixing plus baseline regeneration (the baseline mechanism itself remains correct). | Done locally; pending explicit Linear sync |
| TAL-93F | Admissions-offering remnant reconciliation - DIAGNOSED 2026-07-09 = RETIRE | Diagnosis done (read-only). AdmissionOffering/AdmissionCapacityPlan/AdmissionCapacityReservation/DocumentRequirementItem = superseded pre-rescue "IAS" remnant: their tables were DELETED in rescue commit 23b235b and never recreated (confirmed absent in BOTH tala_db and test_tala_db); no test references them; the live design (ChecklistItem + state-based AdmissionRequirementPolicy + EnrollmentSeatReservation) already covers the need per PRD 3/5/7 + business-evidence benchmark. DECISION = RETIRE, split into TAL-93F1-F3. Model classes are deleted only after all their referencers are removed, so deletions sequence across the sub-slices (pinned per detailed plan). | Diagnosed = RETIRE; F1-F3 DONE (RETIRE complete) |
| TAL-93F1 | RETIRE step 1 - remove orphaned consumer surfaces (low-risk) - DONE | Deleted 30 files of unregistered/unreachable dead code: the AdmissionOfferings/AdmissionCapacityPlans/DocumentRequirementItems Filament resources (24), the AdmissionReadinessDashboard page + AdmissionReadinessDashboardService, the never-constructed AdmissionRequirementResolution DTO, and 3 policies. Factories DEFERRED to F2/F3 with their models (surviving models reference them via @use HasFactory). Verified: route:list 172 unchanged, suite 549 passed, Pint passed, baseline regenerated 393->367 errors (114-line removal, only deletions), phpstan [OK], git diff --check clean. | Done locally; pending explicit Linear sync |
| TAL-93F2 | RETIRE step 2 - unwire the LIVE finance-clearance path + drop capacity services/models (HIGH-RISK finance/enrollment) | Remove the two AdmissionCapacity* calls from EnrollmentFinanceClearanceService.clearIfEligible() (assertReadyForFinanceClearance + secureForFinanceClearedEnrollment); delete AdmissionFinanceReadinessGateService + AdmissionCapacityReservationService + the AdmissionCapacityPlan/AdmissionCapacityReservation models + their factories (AdmissionCapacityPlanFactory + AdmissionCapacityReservationFactory, deferred from F1). Fixes a latent first-term-student crash. Confirm the live EnrollmentSeatReservation flow (EnrollmentPlacementService/EnrollmentGateEvaluator/FinalizeOfficialEnrollment) already enforces capacity; decide whether the readiness gate calendar/scheduling/published-schedule checks are redundant (remove) or must relocate; ADD a first-term finance-clearance test proving no crash. Benchmark cited (PRD 7 + business-evidence). DONE 2026-07-09: removed the 2 calls + 2 deps + unused Payment import, deleted 6 files (2 services, 2 models, 2 factories), added a first-term regression test; full suite 550 passed, baseline 367->338 (all removals), phpstan [OK], route:list 172, git diff --check clean. | Done locally; pending explicit Linear sync |
| TAL-93F3 | RETIRE step 3 - remove the stale policy admin UI + drop remaining models - DONE | DONE 2026-07-09. DECISION = REMOVE (not fix): AdmissionRequirementPolicyResource (8 files) was UNREGISTERED dead code (absent from AdminPanelProvider) built on the retired offering design + broken vs the live model (undefined Status* constants; dead admissionOffering/documentRequirementItems/approver relations). Removed it + deleted the superseded AdmissionOffering + DocumentRequirementItem models + their 2 factories (12 files, no code edits). KEPT the live state-based AdmissionRequirementPolicy + resolver/ApplicantIntakeService/ChecklistItem. The PRD 3.1 + 13.1 admission-requirement-policy CONFIG capability (live model has no working management surface) is BUILT as new core sub-slice TAL-93I (Admission Requirement Policy Configuration, PRD 13.1.1/3.1; depends on TAL-93G), not dropped. Verified: full suite 550 passed; route:list 172; baseline 338->327 errors / 249->239 entries (all removals); phpstan [OK]; Pint + git diff --check clean. | Done locally; pending explicit Linear sync |
| TAL-93G | Canonical Permission Seeding & Role-Access Enforcement (NEW; foundational; HIGH-RISK security) | ROOT-CAUSE fix for the authorization gap: policies correctly check permissions per PRD 2.2.1/2.3, but the seeder creates 7 roles + 0 permissions and nothing assigns permissions to roles, so every can()/canAny() fails and permission-gated surfaces (Applicant Review, Sections, FAQs, ...) are hidden from ALL roles. Fix = create the 25 canonical permissions + assign to the 7 roles per PRD 2.3.1-2.3.7, seed via DatabaseSeeder; add per-role access-matrix tests; fix only policies contradicting PRD 2.3. DECIDED (primary, per PRD 2.3): Registrar=admissions/records/COR/grade-review/sections/schedules; Accounting=assessments/payments/adjustments/promissory; Faculty=encode-grades/own-availability/class-list; Academic Head=authorize-overrides/curricula/faculty-eligibility/availability-review; view-global-records=Registrar+AcademicHead+Accounting; manage-faqs=Super Admin. Super Admin SCOPED per 2.3.7 (config/audit only, NO blanket bypass - separation of duties). Excludes: no 67-policy rewrite; no permissions-management UI. | Done |
| TAL-93H | Done locally - TAL-93H1 + TAL-93H2 + TAL-93H3 all complete (parent complete); pending explicit Linear sync | Scheduling/curriculum static-analysis fixes. Primary factual audit (table existence + AdminPanelProvider registration in test_tala_db) found ALL TAL-93E non-property errors here come from DEAD/PHANTOM pre-rescue surfaces - none is a live add-a-missing-method bug: (1) SectionMeetingAssignmentService = unwired dead manual-assignment path on a phantom section_meetings schema (only assertRecurringBlocksAllow() is live/tested by TAL77); (2) Curriculum/CurriculumSubject/CurriculumReadinessScope = tables MISSING (curriculums, curriculum_subjects, curriculum_readiness_scopes do not exist), CurriculumResource UNREGISTERED (TAL82B asserts assertNotContains), superseded by the live registered CurriculumVersion/CurriculumEntry; (3) DeliveryPattern = table MISSING (delivery_patterns), DeliveryPatternResource UNREGISTERED, no factory/tests. Split (all RETIRE, like TAL-93F): TAL-93H1 = retire the dead SectionMeetingAssignmentService manual path, keep assertRecurringBlocksAllow (done: -8 baseline, 327->319, 572 green); TAL-93H2 = retire the phantom Curriculum/CurriculumSubject/CurriculumReadinessScope cluster + correct the TAL-93G RoleAccessMatrixTest ref (CurriculumResource -> live CurriculumVersionResource) (done: baseline 319->271, 572 green); TAL-93H3 = retire the phantom DeliveryPattern cluster (done: baseline 271->223, 572 green). modalityRequiresRoom is NOT added - its only caller DeliveryPatternService is phantom (retired in H3). No new tables/features. |
| TAL-93I | Admission Requirement Policy Configuration surface (NEW; core MVP config per PRD 13.1.1 #6/#7/#8 + rules 7-8, 3.1) | Deliver the staff-configurable admission-requirement surface the PRD requires as MVP system configuration: rebuild a Filament Resource for the LIVE state-based AdmissionRequirementPolicy (admission_category, credential_basis, requirement_type, evidence_method, blocking_level, effective_from/until, state, authority) + register it in AdminPanelProvider, gated on manage-admission-setup (seeded by TAL-93G) via the kept AdmissionRequirementPolicyPolicy. CORE (not the deferrable notification-template case): PRD 13.1 rule 6 (dependent workflows open after required config complete) + rules 7-8 (flat mapping by category/credential designating blocks-Handover/Enrollment) + 3.1 (checklist depends on configured policies; no code-default). Replaces the broken offering-based Resource removed in TAL-93F3. Depends on TAL-93G; runs before the gate (TAL-93J). Optional default-policy seeder. Add render/create/edit + per-role access tests; regenerate baseline. | Done locally; pending explicit Linear sync |
| TAL-93J1 | Gate Environment and Dependency Preparation (tooling/security; no product behavior) | Established one forced automated-test target on `test_tala_db` while preserving `tala_db` for development/manual validation; added a bootstrap guard before DB-refresh traits, seeded the canonical development baseline idempotently, retired unused `crash-mcp`/`xlsx`, corrected native-CSV authority wording, and applied compatible development-tool security patches. Verification: full audit clean; build, Pint, PHPStan, Composer audit, diff check, and full PHPUnit all passed (587 tests / 5,032 assertions). | Done locally; pending explicit Linear sync |
| TAL-93J2 | Whole-Repository Ground-Truth and Static-Analysis Audit (read-only discovery) | DONE (read-only; no product code changed). Inventory (55 Filament resources vs 38 registered in AdminPanelProvider; ~76 models vs migration-created tables) -> 3 parallel read-only domain workers -> primary independently re-verified via laravel-boost `database-query` on test_tala_db (18 candidate tables ABSENT, 12 live-replacement tables PRESENT) + PRD citations. Classified ~15 SUPERSEDED-REMNANT phantom surfaces (missing table + unregistered resource + only phantom callers, each with a live tabled+tested replacement) -> retire; FaqEntry REQUIRED-BUT-UNBUILT -> build; CorVerification (deferred TAL-88C) and PromissoryNote (contested: live payment wiring) -> decisions. Findings routed to fix boundaries TAL-93J2a-g below. | Done locally (audit complete); pending explicit Linear sync |
| TAL-93J3 | Automated Cross-Role Regression, Security, and UAT-Readiness Gate | After TAL-93J2 findings are resolved or explicitly routed, verify all five staff roles against every registered staff surface according to PRD role ownership; run the complete automated regression/security stack; produce a concise checklist containing only the visual, institutional-workflow, real-email, and later integration validations that genuinely require people. Pure pass/fail; no product fixes inside the gate. CARRY-IN (from TAL-93J2c): `manage-curricula` is assigned (registrar + academic-head) but no live policy checks it after the already-dead `SubjectPolicy` (its sole checker, gating the unregistered phantom SubjectResource) was retired -> J3 surfaces the decision to wire it into the live curriculum resource policies (CourseResource/CourseSpecificationResource/CurriculumVersionResource) OR formally accept role-gating (as FacultyGradeRoster role-gates the former view-class-list capability); if a fix is chosen it becomes a separate bounded boundary, not a product fix inside the gate. | Planned; depends on TAL-93J2 and any accepted fix boundaries; final TAL-93 boundary |

### TAL-93J2 Fix-Boundary Map

TAL-93J2 (read-only audit) is complete; its findings are routed here as separately-approved fix boundaries, planned/executed lowest-risk first BEFORE TAL-93J3. Each retire follows the proven TAL-93F/H phantom-retire process (whole-repo reference sweep -> reference-aware `safe_delete_symbol` -> repoint tests to live replacements, never weaken -> regenerate Larastan baseline -> full green suite on test_tala_db -> cold independent auditor on large deletion sets -> bounded local commit). No code changed yet; every step is separately approved.

| Sub-slice | Nature | Purpose (live replacement / PRD) | Status |
| --- | --- | --- | --- |
| TAL-93J2a | RETIRE (low-risk) | Faculty-availability phantom island (Window/Period/Submission/ChangeRequest + services + unregistered resources) -> CalendarEvent/`calendar_events` (PRD 05 §5.2 line 88 "rather than a submission, locking, version, or change-request workflow"); FacultySubjectEligibility (+Resource/Policy/factory) -> FacultyQualification/`faculty_qualifications` (PRD 05 §5.2); standalone SectionDeliveryGroupResource -> DeliveryGroupsRelationManager under SectionResource. KEEP the SectionDeliveryGroup model+table (aligned/live). Reconcile seeded `manage-faculty-subject-eligibilities`; repoint StaffNavigationVisibilityTest. | DONE locally; pending Linear sync (52 files retired + 4 permissions reconciled; 587 green on test_tala_db; PHPStan [OK]; cold auditor JUSTIFIED; dev tala_db 22/23/0) |
| TAL-93J2b | RETIRE (revised scope 2026-07-10; merged plan + GTG) | Retire the DEAD "grades-table" design (36 files): 4 models (Grade/GradeCorrection/GradeSubmissionPackage(+Item)); 7 dead services/DTOs (GradeEncodingService, GradeSubmissionPackageService, GradeCorrectionService, GradeCorrectionResult, GradeFinalizationService, GradeFinalizationResult, CollegeGradingService); 3 policies; 4 factories; 1 enum (GradeCorrectionStatus); 2 exceptions (InvalidGradeException, MissingGradePeriodException); 15 unregistered Filament resource files. No migration creates the 4 tables (confirmed) -> design is unbacked/dead. KEEP the LIVE grade_rosters design: GradeRoster/GradeRosterRow/GradeOutcomeEvent/LateGradeAuthorization + GradeRosterResource/RowsRelationManager + FacultyGradeRoster page + student GradesView + live services (RecordApprovedGradeCorrection [the correction replacement], GenerateGradeRoster, SubmitGradeRoster, ReturnGradeRoster, PostAndReleaseGradeRoster, RecordIncResolution, SaveGradeRosterPeriodEquivalent, AuthorizeLateGradeEncoding, MarkLateGradeRosters, GradeWindowService, GradePolicyService). Do NOT rebuild GradeCorrection (concept fulfilled by RecordApprovedGradeCorrection). Also unwire dead grade edges from Enrollment (LIVE) + Subject/EnrollmentSubject (phantom; entities kept for J2c) + EnrollmentSubject resource/table/infolist/policy grade actions; remove the dead StudentDashboardService requests.grade_corrections projection (always returns []). RECONCILE 6 now-orphaned permissions (encode-grades, finalize-grades, verify-grade-submissions, manage-grade-corrections, request-grade-corrections, view-grade-submission-progress) in the seeder + tests -> 16 permissions / 17 assignments (Student role -> 0 action permissions). NO PRD change: PRD §2.3 names capabilities not slugs (grep=0); grade capabilities remain via the role-gated GradeRoster workflow; student request-in-TALA is out-of-MVP (PRD 2.3.2/10.4, Registrar records physically-approved corrections). Live couplings VERIFIED SAFE (OperationalReportService uses GradeOutcomeEvent; RowsRelationManager uses RecordApprovedGradeCorrection). Execution: one accountable worker sequentially + independent primary verification + cold auditor; reseed dev tala_db after explicit local-runtime proof; exact dead-vs-shared enforced by reference-aware safe_delete_symbol. | DONE locally; pending Linear sync (36 files retired + 13 edits; reconciled 6 orphaned permissions to 16 canonical / 17 assignments, Student -> 0; full suite 587 green on test_tala_db, PHPStan [OK], baseline pure-removals 118->51 entries / 402 deletions / 0 insertions, route:list 176, zero dangling refs, cold auditor JUSTIFIED; dev tala_db reconciled to 7 roles / 16 permissions / 17 assignments; AppServiceProvider untouched, no schema/migration change) |
| TAL-93J2c | RETIRE (revised scope accepted 2026-07-10; GTG-expanded + Option-A permission prune) | Retire the catalog/enrollment/docs phantom cluster = 28 files, 3 clusters. (1) Subject -> Course+CourseSpecification (PRD 04; TAL82B accepted benchmark): Subject model + SubjectResource dir (8: Resource + Pages Create/Edit/List/View + Schemas Form/Infolist + Table) + SubjectPolicy + SubjectFactory. (2) EnrollmentSubject -> CourseEnrollment/`course_enrollments` (PRD 07 term-offering/section binding): EnrollmentSubject model + EnrollmentSubjectResource dir (5: Resource + Pages List/View + Infolist + Table) + EnrollmentSubjectPolicy + ORPHANED FacultyClassListService + FacultyClassListRow (only caller = deleted EnrollmentSubjectsTable; forSectionSubject queries absent enrollment_subjects/section_teacher + phantom section_meetings columns). (3) DocumentUpload -> DocumentEvidence/`document_evidence`+ChecklistItem (PRD 03 flat checklist): DocumentUpload model + DocumentUploadResource dir (5) + DocumentUploadPolicy + ORPHANED DocumentUploadReviewService (only caller = deleted DocumentUploadsTable; operates on absent document_uploads; superseded by the LIVE ChecklistItemsRelationManager under StudentProfileResource that writes ChecklistItem verification_status). GTG (Boost DB + reference sweeps): subjects/enrollment_subjects/document_uploads ABSENT in test_tala_db+tala_db, no creating migration, resources UNREGISTERED, zero live caller; SubjectSuggestionService imports StudentProfile NOT Subject -> KEEP (excluded); no live-model ::class edge into any phantom model. PERMISSION reconciliation (Option A, user-decided 2026-07-10 = prune the vestigial slug for cleanliness/consistency, removes NO feature): view-class-list becomes vestigial once its only checkers (EnrollmentSubjectPolicy/FacultyClassListService/EnrollmentSubjectResource) are deleted; the live faculty class list is the role-gated FacultyGradeRoster page (PRD 10.5 class-roster table; PRD 01 #30) -> prune view-class-list from DatabaseSeeder -> 15 permissions / 16 assignments, faculty -> 0 action permissions. NO PRD change (capability delivered via role-gated FacultyGradeRoster; slug grep in PRD = 0). approve-documents/evaluate-transferees/manage-curricula/view-global-records remain live (EnrollmentPolicy/StudentProfilePolicy/ApplicantIntakePolicy/Course/Curriculum surfaces). EDITS: RoleAccessMatrixTest (repoint faculty case EnrollmentSubjectResource -> FacultyGradeRoster::class [role-gated, passes with 0 perms]; faculty expected -> []; completeness 16->15); SchemaConformanceTest (16->15 / 17->16); StaffNavigationVisibilityTest (+view-class-list absence guard); TAL82BCurriculumCourseCatalogAcceptanceTest (convert assertNotContains(SubjectResource::class) -> single-quoted FQCN literal so the anti-reintroduction guard survives deletion, per H2 precedent); regenerate phpstan-baseline (expect pure removals). AppServiceProvider NOT touched (registers none of the 3 policies; convention-discovered). No migration/schema/.env. Benchmark: reuse accepted TAL-93F/H/J2a/J2b phantom-retire benchmark + PRD 03/04/07 authority confirmation of live replacements (skip condition met: no new role/record/integration/output/exposed-data/office-decision). Blueprint: no reconciliation (no .md references the 3 resource classes; class-list is a capability fulfilled by FacultyGradeRoster). Execution: primary-direct or one accountable worker -> independent primary verification -> cold auditor -> reseed dev tala_db after the prune (proven APP_ENV=local/DB=tala_db). | DONE locally; pending Linear sync (28 files retired + 6 edits; pruned vestigial view-class-list -> 15 permissions / 16 assignments, faculty -> 0 action permissions; full suite 587 green on test_tala_db, PHPStan [OK], baseline pure-removals 72->61 entries / 54 deletions / 0 insertions, route:list 176, zero dangling refs, cold auditor JUSTIFIED; dev tala_db reconciled to 7 roles / 15 permissions / 16 assignments; AppServiceProvider untouched, no schema/migration change; FOLLOW-UP routed to TAL-93J3: manage-curricula lost its only [already-dead] checker SubjectPolicy and is now assigned-but-unchecked) |
| TAL-93J2d | RETIRE (revised scope accepted 2026-07-10; GTG-verified merged plan, 30 deletions + 2 modifications) | Finance phantom cluster retire = 30 deletions + 2 modifications (verified via Boost DB + Serena reference sweeps). DELETE: (models 3) FeeTemplate, InstallmentPolicy, InstallmentPolicyMilestone; (Filament resource files 21) FeeTemplates/ dir (8: Resource + Pages Create/Edit/List/View + Schemas Form/Infolist + Table), InstallmentPolicies/ dir (8), InstallmentPolicyMilestones/ dir (5: Resource + Pages List/View + Infolist + Table); (policies 3) FeeTemplatePolicy, InstallmentPolicyPolicy, InstallmentPolicyMilestonePolicy; (factory 1) FeeTemplateFactory; (orphaned service 1) InstallmentPolicyService [app/Actions/Finance; ZERO referencing symbols per Serena; queries absent installment_policies/installment_policy_milestones via DB::table -> dead installment-penalty/finance-clearance automation, superseded by the LIVE Assessment/LedgerEntry/PaymentScheduleRow flow]; (orphaned trait 1) HasAccountingConfigurationScope [app/Models/Concerns; referenced ONLY by FeeTemplate+InstallmentPolicy, NOT FeeRule or any live model]. LIVE replacements (present in test_tala_db+tala_db): FeeTemplate -> FeeRule/`fee_rules` + AssessmentLine (PRD 08.4); InstallmentPolicy(+Milestone) -> concrete PaymentScheduleRow/`payment_schedule_rows` + FinancialAccommodation/`financial_accommodations` (PRD 08 §8.5.1/§8.7). GTG (Boost DB + Serena reference sweeps): 3 phantom tables ABSENT in both DBs, NO creating migration, resources NOT in AdminPanelProvider explicit ->resources() list (unregistered -> zero routes -> route:list unchanged at 175, verified route-neutral via pre/post stash comparison; the earlier "176" was a text-line count artifact), InstallmentPolicyService zero refs, trait orphaned. MODIFY (2): TAL68FinanceAssessmentLedgerTest.php (drop `use App\Filament\Resources\FeeTemplates\FeeTemplateResource`; KEEP the FeeRuleResource::shouldRegisterNavigation()=true assertion; convert the FeeTemplateResource::shouldRegisterNavigation()=false assertion to a class-absence string-literal guard assertFalse(class_exists('App\\Filament\\Resources\\FeeTemplates\\FeeTemplateResource')) per TAL82B/J2c anti-reintroduction precedent); regenerate phpstan-baseline.neon (expect pure removals / 0 insertions). NO permission/seeder change (the 3 policies check create-assessments/process-payments/approve-promissory-notes, all remain live-checked by EnrollmentPolicy/PromissoryNotePolicy/PromissoryNoteLifecycleService -> no orphan). NO PRD/blueprint/architecture change (no .md references FeeTemplate/InstallmentPolicy). NO migration/schema/.env; NO dev-DB reseed (no permission change). Benchmark: reuse accepted TAL-68/71/86D/86E finance + TAL-93F/H/J2a/J2b/J2c phantom-retire benchmark + PRD 08 authority; Academico qualified-reference = fee definitions + concrete scheduled payments attached to enrollments, NOT a separate installment-policy template engine (confirms retirement direction only; no Academico code adopted). EXCLUDE PromissoryNote/PromissoryNotePolicy/PromissoryNoteLifecycleService (J2f). Execution: primary-direct -> full verification on test_tala_db -> cold auditor -> acceptance audit -> await Cleanup. | DONE locally; pending Linear sync (30 files retired + 3 edits [TAL68 test repoint, phpstan-baseline regen, this tracker record]; full suite 587 green on test_tala_db / 5044 assertions / 0 failed, PHPStan [OK], baseline pure-removals 42->32 entries / 60 deletions / 0 insertions, route-neutral at 175 [verified via pre/post git stash; earlier "176" was a text-line-count artifact], zero dangling refs [only the intended TAL68 anti-reintroduction string literal], Pint clean, git diff --check clean, cold auditor JUSTIFIED; NO permission/seeder/schema/migration change, NO dev-DB reseed needed; live FeeRule/PaymentScheduleRow/FinancialAccommodation/AssessmentLine + EnrollmentFinanceClearanceService intact) |
| TAL-93J2e | COMPLETE/PATCH (revised scope accepted 2026-07-10; GTG-corrected merged plan; not build-from-scratch) | Complete the partially-built admin-curated FAQ so the PUBLIC LANDING FAQ is dynamic (admin add/edit/delete/publish/reorder; user decision 2026-07-10 = dynamic, not fixed at 3). GTG (Boost `database-query` + code): ALREADY BUILT & KEPT = FaqEntry model (6 categories, is_published, sort_order, created_by/updated_by hooks), FaqEntryPolicy (all gate manage-faqs), FaqEntryResource + Pages(index/create/edit) + FaqEntryForm + FaqEntriesTable, `manage-faqs` seeded + assigned system-super-admin only, RoleAccessMatrix FAQ assertions, the built landing `#faq` Bootstrap accordion markup/styling (3 hardcoded Q&As). ABSENT/GAPS = `faq_entries` table (Boost confirms ABSENT in tala_db+test_tala_db; NO migration exists), FaqEntryResource NOT registered in AdminPanelProvider (FilamentRegistrationStabilizationTest::deferredStaleAdminRoutes asserts `filament.admin.resources.faq-entries.index` ABSENT), no factory/seeder, `/` route serves static view, table wires only EditAction (no DeleteAction). DORMANT (leave as-is): StudentDashboardService::help()/forStudent() has NO live or test caller (pages use BuildCorOutput::forStudent, a different class); PRD 12 page map has NO Student Hub FAQ page -> do NOT build one; adding the table just clears the latent projection. BUILD: `faq_entries` migration (question string; answer text; category string default general; sort_order unsigned int default 0; is_published bool default false; nullable unique `system_key`; created_by/updated_by `foreignId(...)->nullable()->constrained('users')->nullOnDelete()`; index (is_published,sort_order); timestamps) + FaqEntryFactory (+published() state) + idempotent non-overwriting FaqEntrySeeder (seed the 3 current landing questions keyed by system_key so the page is visually identical). MODIFY: register FaqEntryResource in AdminPanelProvider (System Administration group); add DeleteAction to FaqEntriesTable (fulfils user add/delete); add HasFactory + Spatie LogsActivity to FaqEntry (approved include; consistent with CalendarEvent precedent + spatie/laravel-activitylog installed + ActivityResource); change `/` to pass published-ordered FaqEntry records; replace the 3 hardcoded accordion items in welcome.blade.php with an ESCAPED loop (plain-text answers -> the inline Apply link in Q1 becomes plain text, accepted) + restrained empty-state, preserving all styling/anchors/Bootstrap behavior. TEST REPOINTS/ADDS: move `faq entries` in FilamentRegistrationStabilizationTest from deferredStaleAdminRoutes() -> retainedPanelAndFoundationRoutes(); patch SchemaConformanceTest if it enumerates tables; new TAL93J2eFaqEntryAcceptanceTest (schema+indexes+nullable FKs, idempotent non-overwriting seeding, super-admin CRUD incl delete + audit attribution, RBAC denial for other roles, validation/category restriction, published-only + deterministic order public render, unpublished + unsafe-HTML NOT rendered, empty state). DOC UPDATES (approved, bundled): PRD 02 §2.2 item 1 (landing FAQ is dynamic admin-curated published FaqEntry, not static); PRD 02 §2.1.7 (add FAQ/public-content curation to System Super Admin scope); PRD 12 §12.1 (FAQ is landing-only; explicitly no Student Hub FAQ page); ui_surface_blueprint.md (`/` FAQ dynamic admin-managed; FaqEntryResource registered); architecture_specification.md (minor: `/` Blade reads published FaqEntry). Benchmark: standard curated-FAQ (super-admin CRUD, publish/unpublish, deterministic order, escaped public output, provenance) -> native Filament + Blade loop + seeder; NO CMS package/rich-text/search/analytics; Academico has no overlapping FAQ engine. Playwright (user-activated): PRIMARY runs a supplementary non-blocking desktop/mobile landing-accordion smoke at verification; PHPUnit remains the acceptance gate. Route delta: registering the resource ADDS ~3 routes (175 -> ~178) - expected for a build; update any route/resource-count assertion. EXCLUDE: Student Hub/Applicant FAQ page, rich-text/attachments/search/analytics, package/permission changes, unrelated StudentDashboardService work, J2f/J2g, commit/push/Linear. Execution: one accountable worker (Orchestrate) -> independent primary verification + Playwright smoke -> cold auditor -> acceptance audit -> await Cleanup. STOP-RULE for worker: re-confirm via Boost that faq_entries is absent + git clean before writing; if reality differs, stop and re-surface. | DONE locally; pending Linear sync (COMPLETE/PATCH via one accountable worker + independent primary verification + Playwright live smoke; 4 new files [faq_entries migration, FaqEntryFactory, FaqEntrySeeder, TAL93J2eFaqEntryAcceptanceTest] + 14 edits. BUILT: faq_entries table (question/answer/category/sort_order/is_published/nullable-unique system_key/created_by+updated_by foreignId nullOnDelete/index(is_published,sort_order)/timestamps), FaqEntryFactory (+published state), idempotent activity-suppressed FaqEntrySeeder (3 baseline questions keyed by system_key). MODIFIED: registered FaqEntryResource in AdminPanelProvider; added DeleteAction; model +HasFactory +Spatie LogsActivity (CalendarEvent pattern) +scopePublishedOrdered; `/` route passes publishedOrdered(); welcome.blade.php escaped @forelse loop + empty-state (preserved markup/anchors; inline Apply link intentionally dropped to plain text); DatabaseSeeder calls FaqEntrySeeder. TEST REPOINTS: FilamentRegistrationStabilizationTest moved faq-entries deferred->retained; SchemaConformanceTest APPLICATION_TABLES 59->60 (counts 77->78); +8-test acceptance suite. DOCS: PRD 02 §2.2 item1 + §2.1.7, PRD 12 §12.1, ui_surface_blueprint.md, architecture_specification.md. Fixed a real cross-test regression (LogsActivity + committed baseline seed rows broke TAL92E absolute Activity::count) at root cause by suppressing activity logging inside the baseline seeder. VERIFICATION (independent): full suite 595 passed / 5100 assertions / 0 failed on test_tala_db; PHPStan [OK]; phpstan-baseline pure removals 0 insertions / 12 deletions (dropped now-resolvable FaqEntry created_by/updated_by ignores); route:list 178 (175 + 3 FAQ admin routes; retired routes still absent); Pint clean; git diff --check clean; Playwright landing smoke PASS (3 seeded Qs render in DB order, accordion expands, 0 console errors, mobile OK). Dev tala_db reconciled (migrate + idempotent seed = table + 3 published baseline entries, no dupes). DORMANT help()/forStudent left as-is; NO Student Hub FAQ page; NO permission/dependency change; no cold auditor (additive build, not deletion-heavy). Excluded J2f/J2g untouched.) |
| TAL-93J2f | DECISION (diagnose-first) | PromissoryNote CONTESTED: PRD §8.7 concept is required; no `promissory_notes` table; but a full PromissoryNoteLifecycleService + `approve-promissory-notes` permission exist AND it is wired LIVE into PaymentConfirmationService + PayMongoWebhookProcessor (`settleEligibleForEnrollment`, currently a Schema::hasTable-guarded no-op). Primary diagnoses, then USER chooses: retire+untangle (-> FinancialAccommodation `promissory_*` columns) vs finish-build (add table + complete wiring). UNTOUCHED until decided. | Planned; NEEDS USER DECISION |
| TAL-93J2g | RETIRE (decided) | CorVerification deferred scaffold (TAL-88C; PRD 09/01 exclude public COR verification from MVP; no table, no live replacement). USER decided 2026-07-10: RETIRE, cleaning up the ~6 referencing test files (TAL87A/TAL88C/TAL89C/RoleAccessMatrix/StaffNavigationVisibility/TAL72). | Planned (decision=retire) |

After TAL-93J2a-g are resolved (or PromissoryNote explicitly decided), TAL-93J3 (final automated cross-role/security/UAT-readiness gate) runs on the cleaned system.

### Next Boundary

Next primary boundary: **TAL-93J2f** (PromissoryNote DECISION, diagnose-first: PRD §8.7 concept is required; no `promissory_notes` table exists; but a full PromissoryNoteLifecycleService + `approve-promissory-notes` permission exist AND are wired LIVE into PaymentConfirmationService + PayMongoWebhookProcessor as a `Schema::hasTable`-guarded no-op. Primary diagnoses first, then the USER chooses retire+untangle -> FinancialAccommodation `promissory_*` columns vs finish-build add-table+complete-wiring; UNTOUCHED until decided). TAL-93J2a, TAL-93J2b, TAL-93J2c, TAL-93J2d, and TAL-93J2e are DONE locally. TAL-93J2e completed the admin-curated FAQ so the PUBLIC LANDING FAQ is now dynamic/admin-managed (4 new files + 14 edits; faq_entries table + registered FaqEntryResource + DeleteAction + LogsActivity + idempotent audit-suppressed seeder + escaped dynamic welcome loop + 5 doc updates; full suite 595 green on test_tala_db, PHPStan [OK], baseline pure-removals 0 insertions/12 deletions, route:list 178, Playwright landing smoke PASS, dev tala_db reconciled to table + 3 published baseline entries; dormant help()/forStudent left as-is, no Student Hub FAQ page, no permission/dependency change). CARRY-IN to TAL-93J3: `manage-curricula` is now assigned-but-unchecked (its only checker, the already-dead SubjectPolicy, was retired) -> decide wire-into-live-curriculum-policies vs formally-accept-role-gating in J3. Remaining fix boundaries TAL-93J2f-g proceed lowest-risk first. PromissoryNote (TAL-93J2f) is deferred pending primary diagnosis + explicit user decision and stays untouched until then. After TAL-93J2a-g resolve, TAL-93J3 (final automated cross-role/security/UAT-readiness gate) runs. TAL-93A-I + J1 remain done locally, pending explicit Linear sync where applicable. Parent TAL-92 is complete, pending explicit Linear sync.
