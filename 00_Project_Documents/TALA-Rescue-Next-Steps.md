# TALA Rescue Next Steps

## Purpose

This document is the active planning surface for upcoming work. It is reached after `AGENTS.md` and `TALA-Orchestrator-Protocol.md` in the agent intake chain, and it controls issue sequencing rather than product behavior.
- **Issue Numbering:** Always look at the last Issue ID in the `TALA-Local-Linear-Sync-Tracker.md` or on the Linear website. The next issue planned here will start from the subsequent number.
- **The Cycle:**
  1. Primary plans the current issue or sub-slice and waits for acceptance.
  2. If the plan includes a sub-slice split, primary records the approved sub-slice map in this file immediately after acceptance, then plans the first sub-slice.
  3. Implementation/delegation follows `TALA-Orchestrator-Protocol.md`.
  4. Before cleanup or commit, primary reports the protocol acceptance audit.
  5. Move accepted local work to `TALA-Local-Linear-Sync-Tracker.md` as `Done locally; pending explicit Linear sync`.
  6. Create the bounded local Git commit. This standing permission does not authorize push, deploy, PR, or Linear mutation.
  7. Remove completed standalone issues from this file. For parent issues, update the sub-slice map and keep the parent until all sub-slices are complete.
  8. Give the user an acceptance checklist. Patch current-slice defects before advancing.
  9. External Linear synchronization waits for the explicit command `Sync TAL-XX to Linear`.
- **Parent/Sub-slice Tracking:** If a parent issue is split, keep the parent in this document with a compact sub-slice map. Each sub-slice should show its ID, one-line purpose, status, and next boundary. Completed sub-slices are recorded in the local tracker, but the parent remains here until all sub-slices are complete.

Resume rule: after compaction, interruption, rejected worker output, failed/unclear handoff, or stale state, run the short resume checkpoint from `TALA-Orchestrator-Protocol.md` before continuing.

## Planning Rule

Do not duplicate protocol details here. For each active issue, this file should show only the status, goal, sub-slice map when needed, dependency lock, and next boundary. Source order, research/tool rules, benchmark/plugin gates, worker handoff, verification, tracker movement, and commits are controlled by `TALA-Orchestrator-Protocol.md`.

## Active and Upcoming Issues

Dependency lock:

1. Identity, roles, panels, and base administration come first.
2. Academic setup and calendar come before admissions handover because handover assigns Program and Curriculum.
3. Holds and lifecycle foundation come before enrollment because gates and COR visibility depend on student state.
4. Term offerings, resources, and a published Master Schedule come before enrollment binding.
5. Finance core comes before official enrollment because assessment, ledger, downpayment, accommodation, and Finance Gate affect enrollment.
6. Student Hub comes after source records exist; it is a projection, not a source module.
7. A pre-integration regression gate (TAL-93) must pass before integration hardening begins, proving the SIS foundation is clean.
8. CP-SAT and PayMongo end-to-end hardening (TAL-94/95) start only after the pre-integration gate passes; they are human-gated and require credentials/deployment steps.
9. A post-integration regression gate (TAL-96) runs after both integrations are wired in, catching regressions introduced by external service handlers before demo preparation.
10. Demo and rehearsal support (TAL-97) builds only on a fully verified and integration-tested system.

| Issue | Status | Goal |
| --- | --- | --- |
| TAL-91 | Done locally; pending explicit Linear sync | Student Hub Projection Acceptance: validate student-safe views for enrollment, schedule, finance, COR/output, grades, holds, lifecycle, completion, and notices. Split into TAL-91A-D, all complete; see Local Linear Sync Tracker. |
| TAL-92 | Done locally; pending explicit Linear sync | Reports, Audit, Imports, Retention, and Remaining Admin Acceptance: validate fixed reports/exports, audit evidence, guarded imports, retention categories, integration settings, operational monitoring, and remaining system configuration. Owning contract PRD `13_system_admin_reports_audit.md`. Split into TAL-92A–F, all six complete; see Local Linear Sync Tracker. |
| TAL-93 | Planned - split into TAL-93A-J | Cross-Role Regression, Security, and UAT Readiness (Pre-Integration Gate). Sub-slices TAL-93A-G are done locally; TAL-93F (RETIRE the superseded pre-rescue admissions-offering/capacity remnant) is DONE (F1-F3 local); TAL-93H (scheduling/curriculum phantom-surface retires) is DONE (H1-H3 local); TAL-93I (admission requirement policy configuration surface) is DONE (local); only TAL-93J (Cross-Role/Security/UAT gate) remains. See sub-slice map below. Must pass before integration hardening (TAL-94/95) begins. |
| TAL-94 | Planned | CP-SAT End-to-End Scheduling Hardening: prove validated foundation records through solver dispatch, candidate review, publication, schedule visibility, and safe failure/infeasibility handling. Human-gated; requires credentials, solver deployment, and manual verification steps. Deferred items routed here: from TAL-92B — add audit logging (`activity_log`) for solver run records (PRD §13.6 scope 8's solver-run half) once solver dispatch is wired in; from TAL-92A — Solver Run History report (PRD §13.3.4 admin/audit report #6); from TAL-92F: wire the Schedule Released production notification trigger once schedule publication is live. |
| TAL-95 | Planned | Payment Gateway End-to-End Hardening: prove payment attempt, gateway evidence, webhook verification, idempotent ledger posting, Finance Gate, and Accounting/Student visibility. Human-gated; requires API keys, webhook endpoints, and test-mode payment verification. Deferred items routed here: from TAL-92B — add audit logging (`activity_log`) for PayMongo checkout-attempt creation (PRD §13.6 scope 5's PayMongo half) once the live gateway is wired in; from TAL-92A — PayMongo Webhook Event Log report (PRD §13.3.4 admin/audit report #7); from TAL-92F: wire the Payment Received production notification trigger once the live gateway is wired in. |
| TAL-96 | Planned | Cross-Role Regression and Integration Coherence (Post-Integration Gate): verify the full system remains correct after CP-SAT and PayMongo integrations are wired in; catch regressions introduced by external service handlers, solver publication effects, and payment posting side-effects. |
| TAL-97 | Planned | Demo and Rehearsal Support from Verified MVP: rebuild only the realistic demonstration support needed for accepted flows, on top of a fully verified and integration-tested system. |
| TAL-98 | Planned (future enhancement, post-MVP) | Archival & Offline-Storage Management: optional archive-management interface (browse/restore archived and exported records), automated offline/cold-storage export, and on-premise HDD/SSD backup/replication target. Deferred from the TAL-92E retention clarification (PRD §13.7.5 point 7): V1 keeps all records in the operational database via soft-archive (hidden-but-queryable), so this is not required for the capstone MVP. Also the natural home for automated disposal jobs (PRD §13.7.4 rule 10) if the institution later explicitly requires them. Not a dependency for any MVP slice. |
| TAL-99 | Planned (future enhancement, post-MVP) | Data-Subject Privacy Request Handling & Log (RA 10173 §16): intake, DPO triage/fulfilment, and logging of data-subject requests (access, rectification, erasure/blocking, object, data portability, complaint). Deferred from TAL-92F: PRD §13.3.4 admin/audit report #10 (Privacy Request Log) has no source record, and RA 10173 with its IRR mandate no specific in-system request-log UI — data-subject-request handling is a DPO-owned manual/hybrid process. No MVP slice depends on it; access-request evidence is already partly served by `activity_log` + `output_access_logs`, and the erasure/blocking right routes through the TAL-92E hold-aware disposal-review ledger + the TAL-98 archival scope. Not a dependency for any MVP slice. |
| TAL-100 | Planned (future enhancement, post-MVP) | Configurable Notification Templates: admin-editable, database-backed email/notification templates (subject + body per notification type), replacing code-defined content. Deferred from TAL-92F: PRD §13.1.1 configurable record #17. V1 defines notification content in code (Laravel Mailable classes + Blade views); DB-editable templates are a post-MVP administration convenience, not an MVP dependency — no MVP slice requires them. |
| TAL-101 | Planned (future enhancement, post-MVP) | Database-Level Audit Tamper-Evidence Hardening: append-only / write-once protection for the `activity_log` table (e.g., MySQL triggers blocking UPDATE/DELETE, or hash-chaining for cryptographic verifiability). Deferred from TAL-93A (PRD 13.6 note). V1 enforces audit immutability at the application layer only (read-only `ActivityPolicy`, `canCreate()=false`), which the TAL-93A benchmark found proportionate for the capstone MVP; DB-level tamper-evidence is a post-MVP hardening enhancement. Not a dependency for any MVP slice. |

### TAL-93 Sub-Slice Map

Parent TAL-93 (pre-integration gate) is split into ten sub-slices (recorded 2026-07-08; re-sequenced 2026-07-08 after TAL-93B findings). Execution order follows the letters. TAL-93A (housekeeping), TAL-93B (test-isolation repair), TAL-93C (personal-data-correction fix), TAL-93D (verified-user 403 authorization fix), and TAL-93E (static-analysis baseline) are DONE. TAL-93B unmasked two pre-existing GENUINE failures (they fail in isolation - real bugs, not test-plumbing), so feature fixes correctly preceded the gate and restored full-suite green. TAL-93E made whole-project Larastan level 5 usable as a new-error gate after the feature fixes; a later review of its non-property baseline set found ~14 genuine latent bugs, forking into TAL-93F (a large orphaned admissions-offering module: the tableless AdmissionOffering model + stale requirement-policy/document-requirement UI + finance-readiness/dashboard services - a superseded design like the TAL-93C remnant, now DIAGNOSED 2026-07-09 = RETIRE - a git-proven pre-rescue remnant (tables deleted in commit 23b235b; absent in both tala_db and test_tala_db; superseded by the live ChecklistItem/AdmissionRequirementPolicy/EnrollmentSeatReservation design per PRD 3/5/7 + benchmark), split into TAL-93F1-F3 per the rows below) and the scheduling/curriculum fixes (re-lettered TAL-93H). A later cross-role finding added TAL-93G = Canonical Permission Seeding / Role-Access Enforcement (root-cause authorization fix); the TAL-93F3 retire then surfaced that the admission-requirement-policy configuration surface (PRD 13.1.1/3.1, core MVP) needed rebuilding on the live model, added as TAL-93I. TAL-93J (Cross-Role Regression + Security + UAT-readiness) is the substantial gate and runs LAST on a green, working system. Design intent unchanged: the gate (now TAL-93J) stays a pure pass/fail verification layer, while the fixes and decisions live in the earlier slices; each fix slice regenerates the TAL-93E baseline so it stays factual.

| Sub-slice | Nature | Purpose | Status |
| --- | --- | --- | --- |
| TAL-93A | Housekeeping (decision + cleanup) | DONE. (a) Audit-immutability accepted as application-layer for V1 (proportionate per benchmark; the money trail in `ledger_entries` is separately protected by TAL-86C's reversal-ledger), DB-level tamper-evidence routed to post-MVP TAL-101 and the PRD 13.6 note updated. (b) Removed unused `pxlrbt/filament-activity-log`. (c) Removed unused `maatwebsite/excel` and deleted `config/excel.php`; both reconciled in the architecture spec. Removal proven safe (relevant tests 36/440 + isolated 4/4, composer validate, route:list 123, no dangling refs, Pint, diff-check; no app/ PHP changed). Two pre-existing whole-project debts surfaced and routed to TAL-93B (test-isolation, done) and TAL-93E (static-analysis baseline). | Done locally; pending explicit Linear sync |
| TAL-93B | Test-isolation repair (tool-fix) - DONE | DONE (independently verified by primary). Made role-seeding idempotent (`Role::create(['name'=>$x])` -> `Role::findOrCreate($x,'web')`) in 4 test files (StudentHubTest, TAL72GradesFilamentTest, TAL72GradesMvpTest, TAL82AcademicSetupAcceptanceTest), matching the suite convention + laravel-permission-development skill + Boost Laravel 12 docs (RefreshDatabase only transaction-wraps an already-migrated DB, so seeded roles persist and non-idempotent create() collides). Full suite 30 failed -> 9 failed (544 passed, 4859 assertions), 0 RoleAlreadyExists, no regressions. UNMASKED two pre-existing GENUINE failures (fail in isolation), routed to TAL-93C and TAL-93D. | Done locally; pending explicit Linear sync |
| TAL-93C | Feature fix — DONE | DONE. Retired the non-functional legacy PersonalDataCorrectionRequest feature (contradicts PRD §3.5; no migration/table existed; phantom lrn/birthdate fields; wrote to immutable intake; unspecified in PRD). Aligned to PRD §3.5: Registrar Admin-Override now edits all locked identity fields (name + DOB + prior_identifier/LRN); student self-service already existed. Added audit logging to both surfaces (§3.5 rule 2). Blueprint reconciled. New TAL93CProfileUpdateAlignmentTest (2 tests). Benchmark: RA 9048/10172 + DepEd LRN = K-12 optional for college = prior_identifier. Full suite 3 failed (TAL-93D auth only) / 546 passed / 0 skipped. |
| TAL-93D | Feature fix (diagnose-first) - DONE | DONE. Diagnosed the verified-user 403 regression as stale/underspecified student fixtures, not a product authz bug: Student Hub access is intentionally gated by an active official `StudentProfile` after handover. Added StudentProfile fixtures to the positive student cases in `Auth\EmailVerificationBoundaryTest` and `Auth\RoleAwareLoginLandingTest`; production auth code was unchanged. Full suite now green: 549 passed / 4879 assertions. | Done locally; pending explicit Linear sync |
| TAL-93E | Static-analysis baseline (tool-fix, small) - DONE | DONE. Generated `phpstan-baseline.neon` from the current whole-project Larastan level 5 inventory and included it from `phpstan.neon`, so existing debt is accepted and only new errors surface. Live pre-baseline inventory superseded the earlier snapshot: 393 file errors total, 333 `property.notFound`, 60 non-property. Non-property triage found no safe isolated PHP cleanup inside this tool slice; higher-signal items remain captured in the baseline rather than broadening into product fixes. Success proven: whole-project Larastan reports no errors against the baseline. Post-baseline review (during TAL-93F/G planning) reclassified ~14 of the 60 non-property errors as genuine latent bugs - undefined constants/methods on the concrete State* models (AdmissionRequirementPolicy, Section, SectionDeliveryGroup) and a large orphaned tableless AdmissionOffering cluster - routed to TAL-93F + TAL-93G for fixing plus baseline regeneration (the baseline mechanism itself remains correct). | Done locally; pending explicit Linear sync |
| TAL-93F | Admissions-offering remnant reconciliation - DIAGNOSED 2026-07-09 = RETIRE | Diagnosis done (read-only). AdmissionOffering/AdmissionCapacityPlan/AdmissionCapacityReservation/DocumentRequirementItem = superseded pre-rescue "IAS" remnant: their tables were DELETED in rescue commit 23b235b and never recreated (confirmed absent in BOTH tala_db and test_tala_db); no test references them; the live design (ChecklistItem + state-based AdmissionRequirementPolicy + EnrollmentSeatReservation) already covers the need per PRD 3/5/7 + business-evidence benchmark. DECISION = RETIRE, split into TAL-93F1-F3. Model classes are deleted only after all their referencers are removed, so deletions sequence across the sub-slices (pinned per detailed plan). | Diagnosed = RETIRE; F1-F3 DONE (RETIRE complete) |
| TAL-93F1 | RETIRE step 1 - remove orphaned consumer surfaces (low-risk) - DONE | Deleted 30 files of unregistered/unreachable dead code: the AdmissionOfferings/AdmissionCapacityPlans/DocumentRequirementItems Filament resources (24), the AdmissionReadinessDashboard page + AdmissionReadinessDashboardService, the never-constructed AdmissionRequirementResolution DTO, and 3 policies. Factories DEFERRED to F2/F3 with their models (surviving models reference them via @use HasFactory). Verified: route:list 172 unchanged, suite 549 passed, Pint passed, baseline regenerated 393->367 errors (114-line removal, only deletions), phpstan [OK], git diff --check clean. | Done locally; pending explicit Linear sync |
| TAL-93F2 | RETIRE step 2 - unwire the LIVE finance-clearance path + drop capacity services/models (HIGH-RISK finance/enrollment) | Remove the two AdmissionCapacity* calls from EnrollmentFinanceClearanceService.clearIfEligible() (assertReadyForFinanceClearance + secureForFinanceClearedEnrollment); delete AdmissionFinanceReadinessGateService + AdmissionCapacityReservationService + the AdmissionCapacityPlan/AdmissionCapacityReservation models + their factories (AdmissionCapacityPlanFactory + AdmissionCapacityReservationFactory, deferred from F1). Fixes a latent first-term-student crash. Confirm the live EnrollmentSeatReservation flow (EnrollmentPlacementService/EnrollmentGateEvaluator/FinalizeOfficialEnrollment) already enforces capacity; decide whether the readiness gate calendar/scheduling/published-schedule checks are redundant (remove) or must relocate; ADD a first-term finance-clearance test proving no crash. Benchmark cited (PRD 7 + business-evidence). DONE 2026-07-09: removed the 2 calls + 2 deps + unused Payment import, deleted 6 files (2 services, 2 models, 2 factories), added a first-term regression test; full suite 550 passed, baseline 367->338 (all removals), phpstan [OK], route:list 172, git diff --check clean. | Done locally; pending explicit Linear sync |
| TAL-93F3 | RETIRE step 3 - remove the stale policy admin UI + drop remaining models - DONE | DONE 2026-07-09. DECISION = REMOVE (not fix): AdmissionRequirementPolicyResource (8 files) was UNREGISTERED dead code (absent from AdminPanelProvider) built on the retired offering design + broken vs the live model (undefined Status* constants; dead admissionOffering/documentRequirementItems/approver relations). Removed it + deleted the superseded AdmissionOffering + DocumentRequirementItem models + their 2 factories (12 files, no code edits). KEPT the live state-based AdmissionRequirementPolicy + resolver/ApplicantIntakeService/ChecklistItem. The PRD 3.1 + 13.1 admission-requirement-policy CONFIG capability (live model has no working management surface) is BUILT as new core sub-slice TAL-93I (Admission Requirement Policy Configuration, PRD 13.1.1/3.1; depends on TAL-93G), not dropped. Verified: full suite 550 passed; route:list 172; baseline 338->327 errors / 249->239 entries (all removals); phpstan [OK]; Pint + git diff --check clean. | Done locally; pending explicit Linear sync |
| TAL-93G | Canonical Permission Seeding & Role-Access Enforcement (NEW; foundational; HIGH-RISK security) | ROOT-CAUSE fix for the authorization gap: policies correctly check permissions per PRD 2.2.1/2.3, but the seeder creates 7 roles + 0 permissions and nothing assigns permissions to roles, so every can()/canAny() fails and permission-gated surfaces (Applicant Review, Sections, FAQs, ...) are hidden from ALL roles. Fix = create the 25 canonical permissions + assign to the 7 roles per PRD 2.3.1-2.3.7, seed via DatabaseSeeder; add per-role access-matrix tests; fix only policies contradicting PRD 2.3. DECIDED (primary, per PRD 2.3): Registrar=admissions/records/COR/grade-review/sections/schedules; Accounting=assessments/payments/adjustments/promissory; Faculty=encode-grades/own-availability/class-list; Academic Head=authorize-overrides/curricula/faculty-eligibility/availability-review; view-global-records=Registrar+AcademicHead+Accounting; manage-faqs=Super Admin. Super Admin SCOPED per 2.3.7 (config/audit only, NO blanket bypass - separation of duties). Excludes: no 67-policy rewrite; no permissions-management UI. | Done |
| TAL-93H | Done locally - TAL-93H1 + TAL-93H2 + TAL-93H3 all complete (parent complete); pending explicit Linear sync | Scheduling/curriculum static-analysis fixes. Primary factual audit (table existence + AdminPanelProvider registration in test_tala_db) found ALL TAL-93E non-property errors here come from DEAD/PHANTOM pre-rescue surfaces - none is a live add-a-missing-method bug: (1) SectionMeetingAssignmentService = unwired dead manual-assignment path on a phantom section_meetings schema (only assertRecurringBlocksAllow() is live/tested by TAL77); (2) Curriculum/CurriculumSubject/CurriculumReadinessScope = tables MISSING (curriculums, curriculum_subjects, curriculum_readiness_scopes do not exist), CurriculumResource UNREGISTERED (TAL82B asserts assertNotContains), superseded by the live registered CurriculumVersion/CurriculumEntry; (3) DeliveryPattern = table MISSING (delivery_patterns), DeliveryPatternResource UNREGISTERED, no factory/tests. Split (all RETIRE, like TAL-93F): TAL-93H1 = retire the dead SectionMeetingAssignmentService manual path, keep assertRecurringBlocksAllow (done: -8 baseline, 327->319, 572 green); TAL-93H2 = retire the phantom Curriculum/CurriculumSubject/CurriculumReadinessScope cluster + correct the TAL-93G RoleAccessMatrixTest ref (CurriculumResource -> live CurriculumVersionResource) (done: baseline 319->271, 572 green); TAL-93H3 = retire the phantom DeliveryPattern cluster (done: baseline 271->223, 572 green). modalityRequiresRoom is NOT added - its only caller DeliveryPatternService is phantom (retired in H3). No new tables/features. |
| TAL-93I | Admission Requirement Policy Configuration surface (NEW; core MVP config per PRD 13.1.1 #6/#7/#8 + rules 7-8, 3.1) | Deliver the staff-configurable admission-requirement surface the PRD requires as MVP system configuration: rebuild a Filament Resource for the LIVE state-based AdmissionRequirementPolicy (admission_category, credential_basis, requirement_type, evidence_method, blocking_level, effective_from/until, state, authority) + register it in AdminPanelProvider, gated on manage-admission-setup (seeded by TAL-93G) via the kept AdmissionRequirementPolicyPolicy. CORE (not the deferrable notification-template case): PRD 13.1 rule 6 (dependent workflows open after required config complete) + rules 7-8 (flat mapping by category/credential designating blocks-Handover/Enrollment) + 3.1 (checklist depends on configured policies; no code-default). Replaces the broken offering-based Resource removed in TAL-93F3. Depends on TAL-93G; runs before the gate (TAL-93J). Optional default-policy seeder. Add render/create/edit + per-role access tests; regenerate baseline. | Done locally; pending explicit Linear sync |
| TAL-93J | Cross-role regression + security + UAT-readiness gate (the actual gate; substantial) | Main pre-integration verification layer: run the full system across all five staff roles, verify authorization on every surface (every PRD-13 admin surface stays Super-Admin-only), produce a UAT-readiness checklist. Pure pass/fail; actions/decisions live in earlier slices. Depends on TAL-93F + TAL-93G(auth) + TAL-93H(scheduling) + TAL-93I(admission-req config) resolved (system green) + TAL-93E baseline. Scope to be defined with user. LAST slice of TAL-93. | Planned (scope to be defined) |
### Next Boundary

Next primary boundary: TAL-93J (Cross-Role Regression + Security + UAT-readiness gate) - the LAST slice of TAL-93, run on the now-green system (full suite 587). TAL-93I (admission requirement policy configuration surface) is COMPLETE (done locally). Parent TAL-93H is COMPLETE - TAL-93H1 + TAL-93H2 + TAL-93H3 all done locally (phantom-surface retires); TAL-93A-G also done locally (TAL-93F RETIRE complete; TAL-93G authorization fix complete), pending explicit Linear sync. Then TAL-93J (Cross-Role/Security/UAT gate) runs last on the green system. Parent TAL-92 complete, pending explicit Linear sync.
